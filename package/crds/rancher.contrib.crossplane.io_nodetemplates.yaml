---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: nodetemplates.rancher.contrib.crossplane.io
spec:
  group: rancher.contrib.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - rancher
    kind: NodeTemplate
    listKind: NodeTemplateList
    plural: nodetemplates
    singular: nodetemplate
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NodeTemplate is the Schema for the NodeTemplates API. |Provides
          a Rancher v2 Node Template resource. This can be used to create Node Template
          for Rancher v2 and retrieve their information.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NodeTemplateSpec defines the desired state of NodeTemplate
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  amazonec2Config:
                    description: AWS config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        accessKeySecretRef:
                          description: |-
                            AWS access key. Required on Rancher v2.0.x and v2.1.x. Use rancher2_cloud_credential from Rancher v2.2.x (string)
                            AWS Access Key
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        ami:
                          description: |-
                            AWS machine image (string)
                            AWS machine image
                          type: string
                        blockDurationMinutes:
                          description: |-
                            AWS spot instance duration in minutes (60, 120, 180, 240, 300, or 360). Default 0 (string)
                            AWS spot instance duration in minutes (60, 120, 180, 240, 300, or 360)
                          type: string
                        deviceName:
                          description: |-
                            AWS root device name. Default /dev/sda1 (string)
                            AWS root device name
                          type: string
                        encryptEbsVolume:
                          description: |-
                            Encrypt EBS volume. Default false (bool)
                            Encrypt EBS volume
                          type: boolean
                        endpoint:
                          description: |-
                            Optional endpoint URL (hostname only or fully qualified URI) (string)
                            Optional endpoint URL (hostname only or fully qualified URI)
                          type: string
                        httpEndpoint:
                          description: |-
                            Enables or disables the HTTP metadata endpoint on your instances (string)
                            Enables or disables the HTTP metadata endpoint on your instances
                          type: string
                        httpTokens:
                          description: |-
                            The state of token usage for your instance metadata requests (string)
                            The state of token usage for your instance metadata requests
                          type: string
                        iamInstanceProfile:
                          description: |-
                            AWS IAM Instance Profile (string)
                            AWS IAM Instance Profile
                          type: string
                        insecureTransport:
                          description: |-
                            Disable SSL when sending requests (bool)
                            Disable SSL when sending requests
                          type: boolean
                        instanceType:
                          description: |-
                            AWS instance type. Default t2.micro (string)
                            AWS instance type
                          type: string
                        kmsKey:
                          description: |-
                            Custom KMS key ID using the AWS Managed CMK (string)
                            Custom KMS key ID using the AWS Managed CMK
                          type: string
                        monitoring:
                          description: |-
                            Set this flag to enable CloudWatch monitoring. Deafult false (bool)
                            Set this flag to enable CloudWatch monitoring
                          type: boolean
                        openPort:
                          description: |-
                            Make the specified port number accessible from the Internet. (list)
                            Make the specified port number accessible from the Internet
                          items:
                            type: string
                          type: array
                        privateAddressOnly:
                          description: |-
                            Only use a private IP address. Default false (bool)
                            Only use a private IP address
                          type: boolean
                        region:
                          description: |-
                            AWS region. (string)
                            AWS Region
                          type: string
                        requestSpotInstance:
                          description: |-
                            Set this flag to request spot instance. Default false (bool)
                            Set this flag to request spot instance
                          type: boolean
                        retries:
                          description: |-
                            Set retry count for recoverable failures (use -1 to disable). Default 5 (string)
                            Set retry count for recoverable failures (use -1 to disable)
                          type: string
                        rootSize:
                          description: |-
                            AWS root disk size (in GB). Default 16 (string)
                            AWS root disk size (in GB)
                          type: string
                        secretKeySecretRef:
                          description: |-
                            AWS secret key. Required on Rancher v2.0.x and v2.1.x. Use rancher2_cloud_credential from Rancher v2.2.x (string)
                            AWS Secret Key
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        securityGroup:
                          description: |-
                            AWS VPC security group. (list)
                            AWS VPC security group
                          items:
                            type: string
                          type: array
                        securityGroupReadonly:
                          description: |-
                            Skip adding default rules to security groups (bool)
                            Skip adding default rules to security groups
                          type: boolean
                        sessionTokenSecretRef:
                          description: |-
                            AWS Session Token (string)
                            AWS Session Token
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        spotPrice:
                          description: |-
                            AWS spot instance bid price (in dollar). Default 0.50 (string)
                            AWS spot instance bid price (in dollar)
                          type: string
                        sshKeypath:
                          description: SSH Key for Instance
                          type: string
                        sshUser:
                          description: |-
                            Set the name of the ssh user (string)
                            Set the name of the ssh user
                          type: string
                        subnetId:
                          description: |-
                            AWS VPC subnet id (string)
                            AWS VPC subnet id
                          type: string
                        tags:
                          description: |-
                            AWS Tags (e.g. key1,value1,key2,value2) (string)
                            AWS Tags (e.g. key1,value1,key2,value2)
                          type: string
                        useEbsOptimizedInstance:
                          description: |-
                            Create an EBS optimized instance. Default false (bool)
                            Create an EBS optimized instance
                          type: boolean
                        usePrivateAddress:
                          description: |-
                            Force the usage of private IP address. Default false (bool)
                            Force the usage of private IP address
                          type: boolean
                        userdata:
                          description: |-
                            Path to file with cloud-init user data (string)
                            Path to file with cloud-init user data
                          type: string
                        volumeType:
                          description: |-
                            Amazon EBS volume type. Default gp2 (string)
                            Amazon EBS volume type
                          type: string
                        vpcId:
                          description: |-
                            AWS VPC id. (string)
                            AWS VPC id
                          type: string
                        zone:
                          description: |-
                            AWS zone for instance (i.e. a,b,c,d,e) (string)
                            AWS zone for instance (i.e. a,b,c,d,e)
                          type: string
                      type: object
                    type: array
                  annotations:
                    additionalProperties:
                      type: string
                    description: |-
                      Annotations for Node Template object (map)
                      Annotations of the resource
                    type: object
                    x-kubernetes-map-type: granular
                  authCertificateAuthoritySecretRef:
                    description: Auth certificate authority for the Node Template
                      (string)
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  authKeySecretRef:
                    description: Auth key for the Node Template (string)
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  azureConfig:
                    description: Azure config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        acceleratedNetworking:
                          description: Enable Accelerated Networking when creating
                            an Azure Network Interface
                          type: boolean
                        availabilitySet:
                          description: |-
                            Azure Availability Set to place the virtual machine into. Default docker-machine (string)
                            Azure Availability Set to place the virtual machine into
                          type: string
                        availabilityZone:
                          description: |-
                            OpenStack availability zone (string)
                            The Azure Availability Zone the VM should be created in
                          type: string
                        clientIdSecretRef:
                          description: |-
                            Azure Service Principal Account ID. Mandatory on Rancher v2.0.x and v2.1.x. Use rancher2_cloud_credential from Rancher v2.2.x (string)
                            Azure Service Principal Account ID (optional, browser auth is used if not specified)
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        clientSecretSecretRef:
                          description: |-
                            Azure Service Principal Account password. Mandatory on Rancher v2.0.x and v2.1.x. Use rancher2_cloud_credential from Rancher v2.2.x (string)
                            Azure Service Principal Account password (optional, browser auth is used if not specified)
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        customData:
                          description: |-
                            Path to file with custom-data (string)
                            Path to file with custom-data
                          type: string
                        diskSize:
                          description: |-
                            Disk size if using managed disk. For Rancher v2.3.x and above. Default 30 (string)
                            Disk size if using managed disk
                          type: string
                        dns:
                          description: |-
                            A unique DNS label for the public IP adddress (string)
                            A unique DNS label for the public IP adddress
                          type: string
                        dockerPort:
                          description: |-
                            Port number for Docker engine. Default 2376 (string)
                            Port number for Docker engine
                          type: string
                        environment:
                          description: |-
                            Azure environment (e.g. AzurePublicCloud, AzureChinaCloud). Default AzurePublicCloud (string)
                            Azure environment (e.g. AzurePublicCloud, AzureChinaCloud)
                          type: string
                        faultDomainCount:
                          description: |-
                            Fault domain count to use for availability set. Default 3 (string)
                            Fault domain count to use for availability set
                          type: string
                        image:
                          description: |-
                            Azure virtual machine OS image. Default canonical:ubuntu-24_04-lts:server-gen1:latest (string)
                            Azure virtual machine OS image
                          type: string
                        location:
                          description: |-
                            Azure region to create the virtual machine. Default westus (string)
                            Azure region to create the virtual machine
                          type: string
                        managedDisks:
                          description: |-
                            Configures VM and availability set for managed disks. For Rancher v2.3.x and above. Default false (bool)
                            Configures VM and availability set for managed disks
                          type: boolean
                        noPublicIp:
                          description: |-
                            Do not create a public IP address for the machine. Default false (bool)
                            Do not create a public IP address for the machine
                          type: boolean
                        nsg:
                          description: |-
                            Azure Network Security Group to assign this node to (accepts either a name or resource ID, default is to create a new NSG for each machine). Default docker-machine-nsg (string)
                            Azure Network Security Group to assign this node to (accepts either a name or resource ID, default is to create a new NSG for each machine)
                          type: string
                        openPort:
                          description: |-
                            Make the specified port number accessible from the Internet. (list)
                            Make the specified port number accessible from the Internet
                          items:
                            type: string
                          type: array
                        plan:
                          description: |-
                            Azure marketplace purchase plan for Azure Virtual Machine. Format is <publisher>:<product>:<plan>. For Rancher v2.6.3 and above. (string)
                            Purchase plan for Azure Virtual Machine (in <publisher>:<product>:<plan> format)
                          type: string
                        privateIpAddress:
                          description: |-
                            Specify a static private IP address for the machine. (string)
                            Specify a static private IP address for the machine
                          type: string
                        resourceGroup:
                          description: |-
                            Azure Resource Group name (will be created if missing). Default docker-machine (string)
                            Azure Resource Group name (will be created if missing)
                          type: string
                        size:
                          description: |-
                            Size for Azure Virtual Machine. Default Standard_D2_v2 (string)
                            Size for Azure Virtual Machine
                          type: string
                        sshUser:
                          description: |-
                            Set the name of the ssh user (string)
                            Username for SSH login
                          type: string
                        staticPublicIp:
                          description: |-
                            Assign a static public IP address to the machine. Default false (bool)
                            Assign a static public IP address to the machine
                          type: boolean
                        storageType:
                          description: |-
                            Type of Storage Account to host the OS Disk for the machine. Default Standard_LRS (string)
                            Type of Storage Account to host the OS Disk for the machine
                          type: string
                        subnet:
                          description: |-
                            Azure Subnet Name to be used within the Virtual Network. Default docker-machine (string)
                            Azure Subnet Name to be used within the Virtual Network
                          type: string
                        subnetPrefix:
                          description: |-
                            Private CIDR block to be used for the new subnet, should comply RFC 1918. Default 192.168.0.0/16 (string)
                            Private CIDR block to be used for the new subnet, should comply RFC 1918
                          type: string
                        subscriptionIdSecretRef:
                          description: |-
                            Azure Subscription ID. Mandatory on Rancher v2.0.x and v2.1.x. Use rancher2_cloud_credential from Rancher v2.2.x (string)
                            Azure Subscription ID
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        tags:
                          description: |-
                            AWS Tags (e.g. key1,value1,key2,value2) (string)
                            Tags to be applied to the Azure VM instance (e.g. key1,value1,key2,value2)
                          type: string
                        updateDomainCount:
                          description: |-
                            Update domain count to use for availability set. Default 5 (string)
                            Update domain count to use for availability set
                          type: string
                        usePrivateIp:
                          description: |-
                            Use private IP address of the machine to connect. Default false (bool)
                            Use private IP address of the machine to connect
                          type: boolean
                        usePublicIpStandardSku:
                          description: Use the Standard SKU when creating a public
                            IP for an Azure VM
                          type: boolean
                        vnet:
                          description: |-
                            Azure Virtual Network name to connect the virtual machine (in [resourcegroup:]name format). Default docker-machine-vnet (string)
                            Azure Virtual Network name to connect the virtual machine (in [resourcegroup:]name format)
                          type: string
                      type: object
                    type: array
                  cloudCredentialId:
                    description: Cloud credential ID for the Node Template. Required
                      from Rancher v2.2.x (string)
                    type: string
                  cloudCredentialIdRef:
                    description: Reference to a CloudCredential in rancher to populate
                      cloudCredentialId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  cloudCredentialIdSelector:
                    description: Selector for a CloudCredential in rancher to populate
                      cloudCredentialId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  description:
                    description: Description for the Node Template (string)
                    type: string
                  digitaloceanConfig:
                    description: Digitalocean config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        accessTokenSecretRef:
                          description: |-
                            Digital Ocean access token. Mandatory on Rancher v2.0.x and v2.1.x. Use rancher2_cloud_credential from Rancher v2.2.x (string)
                            Digital Ocean access token
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        backups:
                          description: |-
                            Enable backups for droplet. Default false (bool)
                            Enable backups for droplet
                          type: boolean
                        image:
                          description: |-
                            Azure virtual machine OS image. Default canonical:ubuntu-24_04-lts:server-gen1:latest (string)
                            Digital Ocean Image
                          type: string
                        ipv6:
                          description: |-
                            Enable ipv6 for droplet. Default false (bool)
                            Enable ipv6 for droplet
                          type: boolean
                        monitoring:
                          description: |-
                            Set this flag to enable CloudWatch monitoring. Deafult false (bool)
                            Enable monitoring for droplet
                          type: boolean
                        privateNetworking:
                          description: |-
                            Enable private networking for droplet. Default false (bool)
                            Enable private networking for droplet
                          type: boolean
                        region:
                          description: |-
                            AWS region. (string)
                            Digital Ocean region
                          type: string
                        size:
                          description: |-
                            Size for Azure Virtual Machine. Default Standard_D2_v2 (string)
                            Digital Ocean size
                          type: string
                        sshKeyFingerprintSecretRef:
                          description: |-
                            SSH key fingerprint (string)
                            SSH key fingerprint
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        sshKeyPath:
                          description: |-
                            SSH private key path (string)
                            SSH private key path
                          type: string
                        sshPort:
                          description: |-
                            SSH port. Default 22 (string)
                            SSH port
                          type: string
                        sshUser:
                          description: |-
                            Set the name of the ssh user (string)
                            SSH username
                          type: string
                        tags:
                          description: |-
                            AWS Tags (e.g. key1,value1,key2,value2) (string)
                            Comma-separated list of tags to apply to the Droplet
                          type: string
                        userdata:
                          description: |-
                            Path to file with cloud-init user data (string)
                            Path to file with cloud-init user-data
                          type: string
                      type: object
                    type: array
                  driverId:
                    description: The node driver id used by the node template. It's
                      required if the node driver isn't built in Rancher (string)
                    type: string
                  driverIdRef:
                    description: Reference to a NodeDriver in rancher to populate
                      driverId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  driverIdSelector:
                    description: Selector for a NodeDriver in rancher to populate
                      driverId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  engineEnv:
                    additionalProperties:
                      type: string
                    description: Engine environment for the node template (string)
                    type: object
                    x-kubernetes-map-type: granular
                  engineInsecureRegistry:
                    description: Insecure registry for the node template (list)
                    items:
                      type: string
                    type: array
                  engineInstallUrl:
                    description: Docker engine install URL for the node template.
                      Available install docker versions at https://github.com/rancher/install-docker
                      (string)
                    type: string
                  engineLabel:
                    additionalProperties:
                      type: string
                    description: Engine label for the node template (string)
                    type: object
                    x-kubernetes-map-type: granular
                  engineOpt:
                    additionalProperties:
                      type: string
                    description: Engine options for the node template (map)
                    type: object
                    x-kubernetes-map-type: granular
                  engineRegistryMirror:
                    description: Engine registry mirror for the node template (list)
                    items:
                      type: string
                    type: array
                  engineStorageDriver:
                    description: Engine storage driver for the node template (string)
                    type: string
                  harvesterConfig:
                    description: Harvester config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        cpuCount:
                          description: |-
                            CPU count, Default 2 (string)
                            CPU count
                          type: string
                        diskBus:
                          description: |-
                            (Deprecated) Use disk_info instead
                            Disk bus
                          type: string
                        diskInfo:
                          description: |-
                            A JSON string specifying info for the disks e.g. {\"disks\":[{\"imageName\":\"harvester-public/image-57hzg\",\"bootOrder\":1,\"size\":40},{\"storageClassName\":\"node-driver-test\",\"bootOrder\":2,\"size\":1}]} (string)
                            A JSON string specifying info for the disks e.g. `{"disks":[{"imageName":"harvester-public/image-57hzg","bootOrder":1,"size":40},{"storageClassName":"node-driver-test","bootOrder":2,"size":1}]}`
                          type: string
                        diskSize:
                          description: |-
                            Disk size if using managed disk. For Rancher v2.3.x and above. Default 30 (string)
                            Disk size (in GiB)
                          type: string
                        imageName:
                          description: |-
                            (Deprecated) Use disk_info instead
                            Image name
                          type: string
                        memorySize:
                          description: |-
                            Memory size (in GiB), Default 4 (string)
                            Memory size (in GiB)
                          type: string
                        networkData:
                          description: |-
                            NetworkData content of cloud-init, base64 is supported (string)
                            NetworkData content of cloud-init, base64 is supported
                          type: string
                        networkInfo:
                          description: |-
                            A JSON string specifying info for the networks e.g. {\"interfaces\":[{\"networkName\":\"harvester-public/vlan1\"},{\"networkName\":\"harvester-public/vlan2\"}]} (string)
                            A JSON string specifying info for the networks e.g. `{"interfaces":[{"networkName":"harvester-public/vlan1"},{"networkName":"harvester-public/vlan2"}]}`
                          type: string
                        networkModel:
                          description: |-
                            (Deprecated) Use network_info instead
                            Network model
                          type: string
                        networkName:
                          description: |-
                            (Deprecated) Use network_info instead
                            Network name
                          type: string
                        sshPasswordSecretRef:
                          description: |-
                            SSH password (string)
                            SSH password
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        sshUser:
                          description: |-
                            Set the name of the ssh user (string)
                            SSH username
                          type: string
                        userData:
                          description: |-
                            UserData content of cloud-init, base64 is supported. If the image does not contain the qemu-guest-agent package, you must install and start qemu-guest-agent using userdata (string)
                            UserData content of cloud-init, base64 is supported. If the image does not contain the qemu-guest-agent package, you must install and start qemu-guest-agent using userdata
                          type: string
                        vmAffinity:
                          description: |-
                            Virtual machine affinity, only base64 format is supported. For Rancher v2.6.7 and above (string)
                            VM affinity, base64 is supported
                          type: string
                        vmNamespace:
                          description: |-
                            Virtual machine namespace e.g. default (string)
                            Virtual machine namespace
                          type: string
                      type: object
                    type: array
                  hetznerConfig:
                    description: Hetzner config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        apiTokenSecretRef:
                          description: |-
                            Hetzner Cloud project API token (string)
                            Hetzner Cloud project API token
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        image:
                          description: |-
                            Azure virtual machine OS image. Default canonical:ubuntu-24_04-lts:server-gen1:latest (string)
                            Hetzner Cloud server image
                          type: string
                        networks:
                          description: |-
                            Comma-separated list of network IDs or names which should be attached to the server private network interface (string)
                            Comma-separated list of network IDs or names which should be attached to the server private network interface
                          type: string
                        serverLabels:
                          additionalProperties:
                            type: string
                          description: |-
                            Map of the labels which will be assigned to the server. This argument is only available on Hetzner Docker Node Driver:v3.6.0 and above (map)
                            Map of the labels which will be assigned to the server
                          type: object
                          x-kubernetes-map-type: granular
                        serverLocation:
                          description: |-
                            Hetzner Cloud datacenter. Default nbg1 (string)
                            Hetzner Cloud datacenter
                          type: string
                        serverType:
                          description: |-
                            Hetzner Cloud server type. Default cx11 (string)
                            Hetzner Cloud server type
                          type: string
                        usePrivateNetwork:
                          description: |-
                            Use private network. Default false (bool)
                            Use private network
                          type: boolean
                        userdata:
                          description: |-
                            Path to file with cloud-init user data (string)
                            Path to file with cloud-init user-data
                          type: string
                        volumes:
                          description: |-
                            Comma-separated list of volume IDs or names which should be attached to the server (string)
                            Comma-separated list of volume IDs or names which should be attached to the server
                          type: string
                      type: object
                    type: array
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      Labels for Node Template object (map)
                      Labels of the resource
                    type: object
                    x-kubernetes-map-type: granular
                  linodeConfig:
                    description: Linode config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        authorizedUsers:
                          description: |-
                            Linode user accounts (seperated by commas) whose Linode SSH keys will be permitted root access to the created node. (string)
                            Linode user accounts (seperated by commas) whose Linode SSH keys will be permitted root access to the created node
                          type: string
                        createPrivateIp:
                          description: |-
                            Create private IP for the instance. Default false (bool)
                            Create private IP for the instance
                          type: boolean
                        dockerPort:
                          description: |-
                            Port number for Docker engine. Default 2376 (string)
                            Docker Port
                          type: string
                        image:
                          description: |-
                            Azure virtual machine OS image. Default canonical:ubuntu-24_04-lts:server-gen1:latest (string)
                            Specifies the Linode Instance image which determines the OS distribution and base files
                          type: string
                        instanceType:
                          description: |-
                            AWS instance type. Default t2.micro (string)
                            Specifies the Linode Instance type which determines CPU, memory, disk size, etc.
                          type: string
                        label:
                          description: |-
                            Linode Instance Label. (string)
                            Linode Instance Label
                          type: string
                        region:
                          description: |-
                            AWS region. (string)
                            Specifies the region (location) of the Linode instance
                          type: string
                        rootPassSecretRef:
                          description: |-
                            Root Password (string)
                            Root Password
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        sshPort:
                          description: |-
                            SSH port. Default 22 (string)
                            Linode Instance SSH Port
                          type: string
                        sshUser:
                          description: |-
                            Set the name of the ssh user (string)
                            Specifies the user as which docker-machine should log in to the Linode instance to install Docker.
                          type: string
                        stackscript:
                          description: |-
                            Specifies the Linode StackScript to use to create the instance. (string)
                            Specifies the Linode StackScript to use to create the instance
                          type: string
                        stackscriptData:
                          description: |-
                            A JSON string specifying data for the selected StackScript. (string)
                            A JSON string specifying data for the selected StackScript
                          type: string
                        swapSize:
                          description: |-
                            Linode Instance Swap Size (MB). Default 512 (string)
                            Linode Instance Swap Size (MB)
                          type: string
                        tags:
                          description: |-
                            AWS Tags (e.g. key1,value1,key2,value2) (string)
                            A comma separated list of tags to apply to the the Linode resource
                          type: string
                        tokenSecretRef:
                          description: |-
                            Linode API token. Mandatory on Rancher v2.0.x and v2.1.x. Use rancher2_cloud_credential from Rancher v2.2.x (string)
                            Linode API Token
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        uaPrefix:
                          description: |-
                            Prefix the User-Agent in Linode API calls with some 'product/version' (string)
                            Prefix the User-Agent in Linode API calls with some 'product/version'
                          type: string
                      type: object
                    type: array
                  name:
                    description: The name of the Node Template (string)
                    type: string
                  nodeTaints:
                    description: Node taints. For Rancher v2.3.3 and above (List)
                    items:
                      properties:
                        effect:
                          description: 'Taint effect. Supported values : "NoExecute"
                            | "NoSchedule" | "PreferNoSchedule" (string)'
                          type: string
                        key:
                          description: Taint key (string)
                          type: string
                        timeAdded:
                          description: Taint time added (string)
                          type: string
                        value:
                          description: Taint value (string)
                          type: string
                      type: object
                    type: array
                  opennebulaConfig:
                    description: Opennebula config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        b2dSize:
                          description: Size of the Volatile disk in MB - only for
                            b2d (string)
                          type: string
                        cpu:
                          description: CPU value for the VM (string)
                          type: string
                        devPrefix:
                          description: 'Dev prefix to use for the images. E.g.: ''vd'',
                            ''sd'', ''hd'' (string)'
                          type: string
                        disableVnc:
                          description: VNC is enabled by default. Disable it with
                            this flag (bool)
                          type: boolean
                        diskResize:
                          description: Size of the disk for the VM in MB (string)
                          type: string
                        imageId:
                          description: Image ID to use as the VM OS. Conflicts with
                            image_name (string)
                          type: string
                        imageName:
                          description: (Deprecated) Use disk_info instead
                          type: string
                        imageOwner:
                          description: Owner of the image to use as the VM OS (string)
                          type: string
                        memory:
                          description: Size of the memory for the VM in MB (string)
                          type: string
                        networkId:
                          description: Opennebula network ID to connect the machine
                            to. Conflicts with network_name (string)
                          type: string
                        networkName:
                          description: (Deprecated) Use network_info instead
                          type: string
                        networkOwner:
                          description: Opennebula user ID of the Network to connect
                            the machine to (string)
                          type: string
                        passwordSecretRef:
                          description: Set the password for the XML-RPC API authentication
                            (string)
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        sshUser:
                          description: Set the name of the ssh user (string)
                          type: string
                        templateId:
                          description: Opennebula template ID to use. Conflicts with
                            template_name (string)
                          type: string
                        templateName:
                          description: Name of the Opennbula template to use. Conflicts
                            with template_id (string)
                          type: string
                        user:
                          description: Set the user for the XML-RPC API authentication
                            (string)
                          type: string
                        vcpu:
                          description: VCPUs for the VM (string)
                          type: string
                        xmlRpcUrl:
                          description: Set the url for the Opennebula XML-RPC API
                            (string)
                          type: string
                      type: object
                    type: array
                  openstackConfig:
                    description: Openstack config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        activeTimeout:
                          description: OpenStack active timeout Default 200 (string)
                          type: string
                        applicationCredentialId:
                          description: OpenStack application credential id. Conflicts
                            with application_credential_name (string)
                          type: string
                        applicationCredentialName:
                          description: OpenStack application credential name. Conflicts
                            with application_credential_id (string)
                          type: string
                        applicationCredentialSecretSecretRef:
                          description: OpenStack application credential secret (string)
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        authUrl:
                          description: OpenStack authentication URL (string)
                          type: string
                        availabilityZone:
                          description: OpenStack availability zone (string)
                          type: string
                        bootFromVolume:
                          description: Enable booting from volume. Default is false
                            (bool)
                          type: boolean
                        cacert:
                          description: CA certificate bundle to verify against (string)
                          type: string
                        configDrive:
                          description: Enables the OpenStack config drive for the
                            instance. Default false (bool)
                          type: boolean
                        domainId:
                          description: OpenStack domain ID. Identity v3 only. Conflicts
                            with domain_name (string)
                          type: string
                        domainName:
                          description: OpenStack domain name. Identity v3 only. Conflicts
                            with domain_id (string)
                          type: string
                        endpointType:
                          description: OpenStack endpoint type. adminURL, internalURL
                            or publicURL (string)
                          type: string
                        flavorId:
                          description: OpenStack flavor id to use for the instance.
                            Conflicts with flavor_name (string)
                          type: string
                        flavorName:
                          description: OpenStack flavor name to use for the instance.
                            Conflicts with flavor_id (string)
                          type: string
                        floatingIpPool:
                          description: OpenStack floating IP pool to get an IP from
                            to assign to the instance (string)
                          type: string
                        imageId:
                          description: Image ID to use as the VM OS. Conflicts with
                            image_name (string)
                          type: string
                        imageName:
                          description: (Deprecated) Use disk_info instead
                          type: string
                        insecure:
                          description: Disable TLS credential checking. Default false
                            (bool)
                          type: boolean
                        ipVersion:
                          description: OpenStack version of IP address assigned for
                            the machine Default 4 (string)
                          type: string
                        keypairName:
                          description: OpenStack keypair to use to SSH to the instance
                            (string)
                          type: string
                        netId:
                          description: OpenStack network id the machine will be connected
                            on. Conflicts with net_name (string)
                          type: string
                        netName:
                          description: OpenStack network name the machine will be
                            connected on. Conflicts with net_id (string)
                          type: string
                        novaNetwork:
                          description: Use the nova networking services instead of
                            neutron (string)
                          type: boolean
                        passwordSecretRef:
                          description: Set the password for the XML-RPC API authentication
                            (string)
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        privateKeyFileSecretRef:
                          description: Private key content to use for SSH (string)
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        region:
                          description: AWS region. (string)
                          type: string
                        secGroups:
                          description: OpenStack comma separated security groups for
                            the machine (string)
                          type: string
                        sshPort:
                          description: SSH port. Default 22 (string)
                          type: string
                        sshUser:
                          description: Set the name of the ssh user (string)
                          type: string
                        tenantId:
                          description: OpenStack tenant id. Conflicts with tenant_name
                            (string)
                          type: string
                        tenantName:
                          description: OpenStack tenant name. Conflicts with tenant_id
                            (string)
                          type: string
                        userDataFile:
                          description: File containing an openstack userdata script
                            (string)
                          type: string
                        username:
                          description: OpenStack username (string)
                          type: string
                        volumeDevicePath:
                          description: OpenStack volume device path (attaching). Applicable
                            only when boot_from_volume is true. Omit for auto /dev/vdb.
                            (string)
                          type: string
                        volumeId:
                          description: OpenStack volume id of existing volume. Applicable
                            only when boot_from_volume is true (string)
                          type: string
                        volumeName:
                          description: OpenStack volume name of existing volume. Applicable
                            only when boot_from_volume is true (string)
                          type: string
                        volumeSize:
                          description: OpenStack volume size (GiB). Required when
                            boot_from_volume is true (string)
                          type: string
                        volumeType:
                          description: Amazon EBS volume type. Default gp2 (string)
                          type: string
                      type: object
                    type: array
                  outscaleConfig:
                    description: Outscale config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        accessKeySecretRef:
                          description: |-
                            AWS access key. Required on Rancher v2.0.x and v2.1.x. Use rancher2_cloud_credential from Rancher v2.2.x (string)
                            Outscale Access Key
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        extraTagsAll:
                          description: |-
                            Extra tags for all created resources (e.g. key1=value1,key2=value2) (list)
                            Extra tags for all created resources (e.g. key1=value1,key2=value2)
                          items:
                            type: string
                          type: array
                        extraTagsInstances:
                          description: |-
                            Extra tags only for instances (e.g. key1=value1,key2=value2) (list)
                            Extra tags only for instances (e.g. key1=value1,key2=value2)
                          items:
                            type: string
                          type: array
                        instanceType:
                          description: |-
                            AWS instance type. Default t2.micro (string)
                            Outscale VM type
                          type: string
                        region:
                          description: |-
                            AWS region. (string)
                            Outscale Region
                          type: string
                        rootDiskIops:
                          description: |-
                            Iops for io1 Root Disk. From 1 to 13000.
                            Iops for io1 Root Disk. From 1 to 13000.
                          type: number
                        rootDiskSize:
                          description: |-
                            Size of the Root Disk (in GB). From 1 to 14901.
                            Size of the Root Disk (in GB). From 1 to 14901.
                          type: number
                        rootDiskType:
                          description: |-
                            Type of the Root Disk. Possible values are :'standard', 'gp2' or 'io1'.
                            Type of the Root Disk. Possible values are :'standard', 'gp2' or 'io1'.
                          type: string
                        secretKeySecretRef:
                          description: |-
                            AWS secret key. Required on Rancher v2.0.x and v2.1.x. Use rancher2_cloud_credential from Rancher v2.2.x (string)
                            Outscale Secret Key
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        securityGroupIds:
                          description: |-
                            Ids of user defined Security Groups to add to the machine. (list)
                            Ids of user defined Security Groups to add to the machine
                          items:
                            type: string
                          type: array
                        sourceOmi:
                          description: |-
                            Outscale Machine Image to use as bootstrap for the VM. Default ami-2cf1fa3e (string)
                            Outscale Machine Image to use as bootstrap for the VM
                          type: string
                      type: object
                    type: array
                  useInternalIpAddress:
                    description: Engine storage driver for the node template (bool)
                    type: boolean
                  vsphereConfig:
                    description: vSphere config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        boot2dockerUrl:
                          description: |-
                            vSphere URL for boot2docker iso image. Default https://releases.rancher.com/os/latest/rancheros-vmware.iso (string)
                            vSphere URL for boot2docker image
                          type: string
                        cfgparam:
                          description: |-
                            vSphere vm configuration parameters (used for guestinfo) (list)
                            vSphere vm configuration parameters (used for guestinfo)
                          items:
                            type: string
                          type: array
                        cloneFrom:
                          description: |-
                            If you choose creation type vm (clone vm) a name of what vm you want to clone is required. From Rancher v2.3.3 (string)
                            If you choose creation type clone a name of what you want to clone is required
                          type: string
                        cloudConfig:
                          description: |-
                            Cloud Config YAML content to inject as user-data. From Rancher v2.3.3 (string)
                            Filepath to a cloud-config yaml file to put into the ISO user-data
                          type: string
                        cloudinit:
                          description: |-
                            vSphere cloud-init file or url to set in the guestinfo (string)
                            vSphere cloud-init filepath or url to add to guestinfo
                          type: string
                        contentLibrary:
                          description: |-
                            If you choose to clone from a content library template specify the name of the library. From Rancher v2.3.3 (string)
                            If you choose to clone from a content library template specify the name of the library
                          type: string
                        cpuCount:
                          description: |-
                            CPU count, Default 2 (string)
                            vSphere CPU number for docker VM
                          type: string
                        creationType:
                          description: |-
                            Creation type when creating a new virtual machine. Supported values: vm, template, library, legacy. Default legacy. From Rancher v2.3.3 (string)
                            Creation type when creating a new virtual machine. Supported values: vm, template, library, legacy
                          type: string
                        customAttributes:
                          description: |-
                            vSphere custom attributes, format key/value e.g. 200=my custom value. From Rancher v2.3.3 (List)
                            vSphere custom attributes, format key/value e.g. '200=my custom value'
                          items:
                            type: string
                          type: array
                        datacenter:
                          description: |-
                            vSphere datacenter for docker VM (string)
                            vSphere datacenter for virtual machine
                          type: string
                        datastore:
                          description: |-
                            vSphere datastore for docker VM (string)
                            vSphere datastore for virtual machine
                          type: string
                        datastoreCluster:
                          description: |-
                            vSphere datastore cluster for virtual machine. From Rancher v2.3.3 (string)
                            vSphere datastore cluster for virtual machine
                          type: string
                        diskSize:
                          description: |-
                            Disk size if using managed disk. For Rancher v2.3.x and above. Default 30 (string)
                            vSphere size of disk for docker VM (in MB)
                          type: string
                        folder:
                          description: |-
                            vSphere folder for the docker VM. This folder must already exist in the datacenter (string)
                            vSphere folder for the docker VM. This folder must already exist in the datacenter
                          type: string
                        gracefulShutdownTimeout:
                          description: Duration in seconds before the graceful shutdown
                            of the VM times out and the VM is destroyed. A force destroy
                            will be performed when the value is zero
                          type: string
                        hostsystem:
                          description: |-
                            vSphere compute resource where the docker VM will be instantiated. This can be omitted if using a cluster with DRS (string)
                            vSphere compute resource where the docker VM will be instantiated. This can be omitted if using a cluster with DRS
                          type: string
                        memorySize:
                          description: |-
                            Memory size (in GiB), Default 4 (string)
                            vSphere size of memory for docker VM (in MB)
                          type: string
                        network:
                          description: |-
                            vSphere network where the docker VM will be attached (list)
                            vSphere network where the virtual machine will be attached
                          items:
                            type: string
                          type: array
                        passwordSecretRef:
                          description: |-
                            Set the password for the XML-RPC API authentication (string)
                            vSphere password
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        pool:
                          description: |-
                            vSphere resource pool for docker VM (string)
                            vSphere resource pool for docker VM
                          type: string
                        sshPasswordSecretRef:
                          description: |-
                            SSH password (string)
                            If using a non-B2D image you can specify the ssh password
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        sshPort:
                          description: |-
                            SSH port. Default 22 (string)
                            If using a non-B2D image you can specify the ssh port
                          type: string
                        sshUser:
                          description: |-
                            Set the name of the ssh user (string)
                            If using a non-B2D image you can specify the ssh user
                          type: string
                        sshUserGroup:
                          description: |-
                            If using a non-B2D image the uploaded keys will need chown'ed. Default staff. From Rancher v2.3.3 (string)
                            If using a non-B2D image the uploaded keys will need chown'ed, defaults to staff e.g. docker:staff
                          type: string
                        tags:
                          description: |-
                            AWS Tags (e.g. key1,value1,key2,value2) (string)
                            vSphere tags id e.g. urn:xxx
                          items:
                            type: string
                          type: array
                        username:
                          description: |-
                            OpenStack username (string)
                            vSphere username
                          type: string
                        vappIpAllocationPolicy:
                          description: |-
                            vSphere vApp IP allocation policy. Supported values are: dhcp, fixed, transient and fixedAllocated (string)
                            vSphere vApp IP allocation policy. Supported values are: dhcp, fixed, transient and fixedAllocated
                          type: string
                        vappIpProtocol:
                          description: |-
                            vSphere vApp IP protocol for this deployment. Supported values are: IPv4 and IPv6 (string)
                            vSphere vApp IP protocol for this deployment. Supported values are: IPv4 and IPv6
                          type: string
                        vappProperty:
                          description: |-
                            vSphere vApp properties (list)
                            vSphere vApp properties
                          items:
                            type: string
                          type: array
                        vappTransport:
                          description: |-
                            vSphere OVF environment transports to use for properties. Supported values are: iso and com.vmware.guestInfo (string)
                            vSphere OVF environment transports to use for properties. Supported values are: iso and com.vmware.guestInfo
                          type: string
                        vcenter:
                          description: |-
                            vSphere IP/hostname for vCenter. Mandatory on Rancher v2.0.x and v2.1.x. Use rancher2_cloud_credential from Rancher v2.2.x (string)
                            vSphere IP/hostname for vCenter
                          type: string
                        vcenterPort:
                          description: |-
                            vSphere Port for vCenter. Mandatory on Rancher v2.0.x and v2.1.x. Use rancher2_cloud_credential from Rancher v2.2.x. Default 443 (string)
                            vSphere Port for vCenter
                          type: string
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  amazonec2Config:
                    description: AWS config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        accessKeySecretRef:
                          description: |-
                            AWS access key. Required on Rancher v2.0.x and v2.1.x. Use rancher2_cloud_credential from Rancher v2.2.x (string)
                            AWS Access Key
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        ami:
                          description: |-
                            AWS machine image (string)
                            AWS machine image
                          type: string
                        blockDurationMinutes:
                          description: |-
                            AWS spot instance duration in minutes (60, 120, 180, 240, 300, or 360). Default 0 (string)
                            AWS spot instance duration in minutes (60, 120, 180, 240, 300, or 360)
                          type: string
                        deviceName:
                          description: |-
                            AWS root device name. Default /dev/sda1 (string)
                            AWS root device name
                          type: string
                        encryptEbsVolume:
                          description: |-
                            Encrypt EBS volume. Default false (bool)
                            Encrypt EBS volume
                          type: boolean
                        endpoint:
                          description: |-
                            Optional endpoint URL (hostname only or fully qualified URI) (string)
                            Optional endpoint URL (hostname only or fully qualified URI)
                          type: string
                        httpEndpoint:
                          description: |-
                            Enables or disables the HTTP metadata endpoint on your instances (string)
                            Enables or disables the HTTP metadata endpoint on your instances
                          type: string
                        httpTokens:
                          description: |-
                            The state of token usage for your instance metadata requests (string)
                            The state of token usage for your instance metadata requests
                          type: string
                        iamInstanceProfile:
                          description: |-
                            AWS IAM Instance Profile (string)
                            AWS IAM Instance Profile
                          type: string
                        insecureTransport:
                          description: |-
                            Disable SSL when sending requests (bool)
                            Disable SSL when sending requests
                          type: boolean
                        instanceType:
                          description: |-
                            AWS instance type. Default t2.micro (string)
                            AWS instance type
                          type: string
                        kmsKey:
                          description: |-
                            Custom KMS key ID using the AWS Managed CMK (string)
                            Custom KMS key ID using the AWS Managed CMK
                          type: string
                        monitoring:
                          description: |-
                            Set this flag to enable CloudWatch monitoring. Deafult false (bool)
                            Set this flag to enable CloudWatch monitoring
                          type: boolean
                        openPort:
                          description: |-
                            Make the specified port number accessible from the Internet. (list)
                            Make the specified port number accessible from the Internet
                          items:
                            type: string
                          type: array
                        privateAddressOnly:
                          description: |-
                            Only use a private IP address. Default false (bool)
                            Only use a private IP address
                          type: boolean
                        region:
                          description: |-
                            AWS region. (string)
                            AWS Region
                          type: string
                        requestSpotInstance:
                          description: |-
                            Set this flag to request spot instance. Default false (bool)
                            Set this flag to request spot instance
                          type: boolean
                        retries:
                          description: |-
                            Set retry count for recoverable failures (use -1 to disable). Default 5 (string)
                            Set retry count for recoverable failures (use -1 to disable)
                          type: string
                        rootSize:
                          description: |-
                            AWS root disk size (in GB). Default 16 (string)
                            AWS root disk size (in GB)
                          type: string
                        secretKeySecretRef:
                          description: |-
                            AWS secret key. Required on Rancher v2.0.x and v2.1.x. Use rancher2_cloud_credential from Rancher v2.2.x (string)
                            AWS Secret Key
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        securityGroup:
                          description: |-
                            AWS VPC security group. (list)
                            AWS VPC security group
                          items:
                            type: string
                          type: array
                        securityGroupReadonly:
                          description: |-
                            Skip adding default rules to security groups (bool)
                            Skip adding default rules to security groups
                          type: boolean
                        sessionTokenSecretRef:
                          description: |-
                            AWS Session Token (string)
                            AWS Session Token
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        spotPrice:
                          description: |-
                            AWS spot instance bid price (in dollar). Default 0.50 (string)
                            AWS spot instance bid price (in dollar)
                          type: string
                        sshKeypath:
                          description: SSH Key for Instance
                          type: string
                        sshUser:
                          description: |-
                            Set the name of the ssh user (string)
                            Set the name of the ssh user
                          type: string
                        subnetId:
                          description: |-
                            AWS VPC subnet id (string)
                            AWS VPC subnet id
                          type: string
                        tags:
                          description: |-
                            AWS Tags (e.g. key1,value1,key2,value2) (string)
                            AWS Tags (e.g. key1,value1,key2,value2)
                          type: string
                        useEbsOptimizedInstance:
                          description: |-
                            Create an EBS optimized instance. Default false (bool)
                            Create an EBS optimized instance
                          type: boolean
                        usePrivateAddress:
                          description: |-
                            Force the usage of private IP address. Default false (bool)
                            Force the usage of private IP address
                          type: boolean
                        userdata:
                          description: |-
                            Path to file with cloud-init user data (string)
                            Path to file with cloud-init user data
                          type: string
                        volumeType:
                          description: |-
                            Amazon EBS volume type. Default gp2 (string)
                            Amazon EBS volume type
                          type: string
                        vpcId:
                          description: |-
                            AWS VPC id. (string)
                            AWS VPC id
                          type: string
                        zone:
                          description: |-
                            AWS zone for instance (i.e. a,b,c,d,e) (string)
                            AWS zone for instance (i.e. a,b,c,d,e)
                          type: string
                      type: object
                    type: array
                  annotations:
                    additionalProperties:
                      type: string
                    description: |-
                      Annotations for Node Template object (map)
                      Annotations of the resource
                    type: object
                    x-kubernetes-map-type: granular
                  authCertificateAuthoritySecretRef:
                    description: Auth certificate authority for the Node Template
                      (string)
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  authKeySecretRef:
                    description: Auth key for the Node Template (string)
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  azureConfig:
                    description: Azure config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        acceleratedNetworking:
                          description: Enable Accelerated Networking when creating
                            an Azure Network Interface
                          type: boolean
                        availabilitySet:
                          description: |-
                            Azure Availability Set to place the virtual machine into. Default docker-machine (string)
                            Azure Availability Set to place the virtual machine into
                          type: string
                        availabilityZone:
                          description: |-
                            OpenStack availability zone (string)
                            The Azure Availability Zone the VM should be created in
                          type: string
                        clientIdSecretRef:
                          description: |-
                            Azure Service Principal Account ID. Mandatory on Rancher v2.0.x and v2.1.x. Use rancher2_cloud_credential from Rancher v2.2.x (string)
                            Azure Service Principal Account ID (optional, browser auth is used if not specified)
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        clientSecretSecretRef:
                          description: |-
                            Azure Service Principal Account password. Mandatory on Rancher v2.0.x and v2.1.x. Use rancher2_cloud_credential from Rancher v2.2.x (string)
                            Azure Service Principal Account password (optional, browser auth is used if not specified)
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        customData:
                          description: |-
                            Path to file with custom-data (string)
                            Path to file with custom-data
                          type: string
                        diskSize:
                          description: |-
                            Disk size if using managed disk. For Rancher v2.3.x and above. Default 30 (string)
                            Disk size if using managed disk
                          type: string
                        dns:
                          description: |-
                            A unique DNS label for the public IP adddress (string)
                            A unique DNS label for the public IP adddress
                          type: string
                        dockerPort:
                          description: |-
                            Port number for Docker engine. Default 2376 (string)
                            Port number for Docker engine
                          type: string
                        environment:
                          description: |-
                            Azure environment (e.g. AzurePublicCloud, AzureChinaCloud). Default AzurePublicCloud (string)
                            Azure environment (e.g. AzurePublicCloud, AzureChinaCloud)
                          type: string
                        faultDomainCount:
                          description: |-
                            Fault domain count to use for availability set. Default 3 (string)
                            Fault domain count to use for availability set
                          type: string
                        image:
                          description: |-
                            Azure virtual machine OS image. Default canonical:ubuntu-24_04-lts:server-gen1:latest (string)
                            Azure virtual machine OS image
                          type: string
                        location:
                          description: |-
                            Azure region to create the virtual machine. Default westus (string)
                            Azure region to create the virtual machine
                          type: string
                        managedDisks:
                          description: |-
                            Configures VM and availability set for managed disks. For Rancher v2.3.x and above. Default false (bool)
                            Configures VM and availability set for managed disks
                          type: boolean
                        noPublicIp:
                          description: |-
                            Do not create a public IP address for the machine. Default false (bool)
                            Do not create a public IP address for the machine
                          type: boolean
                        nsg:
                          description: |-
                            Azure Network Security Group to assign this node to (accepts either a name or resource ID, default is to create a new NSG for each machine). Default docker-machine-nsg (string)
                            Azure Network Security Group to assign this node to (accepts either a name or resource ID, default is to create a new NSG for each machine)
                          type: string
                        openPort:
                          description: |-
                            Make the specified port number accessible from the Internet. (list)
                            Make the specified port number accessible from the Internet
                          items:
                            type: string
                          type: array
                        plan:
                          description: |-
                            Azure marketplace purchase plan for Azure Virtual Machine. Format is <publisher>:<product>:<plan>. For Rancher v2.6.3 and above. (string)
                            Purchase plan for Azure Virtual Machine (in <publisher>:<product>:<plan> format)
                          type: string
                        privateIpAddress:
                          description: |-
                            Specify a static private IP address for the machine. (string)
                            Specify a static private IP address for the machine
                          type: string
                        resourceGroup:
                          description: |-
                            Azure Resource Group name (will be created if missing). Default docker-machine (string)
                            Azure Resource Group name (will be created if missing)
                          type: string
                        size:
                          description: |-
                            Size for Azure Virtual Machine. Default Standard_D2_v2 (string)
                            Size for Azure Virtual Machine
                          type: string
                        sshUser:
                          description: |-
                            Set the name of the ssh user (string)
                            Username for SSH login
                          type: string
                        staticPublicIp:
                          description: |-
                            Assign a static public IP address to the machine. Default false (bool)
                            Assign a static public IP address to the machine
                          type: boolean
                        storageType:
                          description: |-
                            Type of Storage Account to host the OS Disk for the machine. Default Standard_LRS (string)
                            Type of Storage Account to host the OS Disk for the machine
                          type: string
                        subnet:
                          description: |-
                            Azure Subnet Name to be used within the Virtual Network. Default docker-machine (string)
                            Azure Subnet Name to be used within the Virtual Network
                          type: string
                        subnetPrefix:
                          description: |-
                            Private CIDR block to be used for the new subnet, should comply RFC 1918. Default 192.168.0.0/16 (string)
                            Private CIDR block to be used for the new subnet, should comply RFC 1918
                          type: string
                        subscriptionIdSecretRef:
                          description: |-
                            Azure Subscription ID. Mandatory on Rancher v2.0.x and v2.1.x. Use rancher2_cloud_credential from Rancher v2.2.x (string)
                            Azure Subscription ID
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        tags:
                          description: |-
                            AWS Tags (e.g. key1,value1,key2,value2) (string)
                            Tags to be applied to the Azure VM instance (e.g. key1,value1,key2,value2)
                          type: string
                        updateDomainCount:
                          description: |-
                            Update domain count to use for availability set. Default 5 (string)
                            Update domain count to use for availability set
                          type: string
                        usePrivateIp:
                          description: |-
                            Use private IP address of the machine to connect. Default false (bool)
                            Use private IP address of the machine to connect
                          type: boolean
                        usePublicIpStandardSku:
                          description: Use the Standard SKU when creating a public
                            IP for an Azure VM
                          type: boolean
                        vnet:
                          description: |-
                            Azure Virtual Network name to connect the virtual machine (in [resourcegroup:]name format). Default docker-machine-vnet (string)
                            Azure Virtual Network name to connect the virtual machine (in [resourcegroup:]name format)
                          type: string
                      type: object
                    type: array
                  cloudCredentialId:
                    description: Cloud credential ID for the Node Template. Required
                      from Rancher v2.2.x (string)
                    type: string
                  cloudCredentialIdRef:
                    description: Reference to a CloudCredential in rancher to populate
                      cloudCredentialId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  cloudCredentialIdSelector:
                    description: Selector for a CloudCredential in rancher to populate
                      cloudCredentialId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  description:
                    description: Description for the Node Template (string)
                    type: string
                  digitaloceanConfig:
                    description: Digitalocean config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        accessTokenSecretRef:
                          description: |-
                            Digital Ocean access token. Mandatory on Rancher v2.0.x and v2.1.x. Use rancher2_cloud_credential from Rancher v2.2.x (string)
                            Digital Ocean access token
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        backups:
                          description: |-
                            Enable backups for droplet. Default false (bool)
                            Enable backups for droplet
                          type: boolean
                        image:
                          description: |-
                            Azure virtual machine OS image. Default canonical:ubuntu-24_04-lts:server-gen1:latest (string)
                            Digital Ocean Image
                          type: string
                        ipv6:
                          description: |-
                            Enable ipv6 for droplet. Default false (bool)
                            Enable ipv6 for droplet
                          type: boolean
                        monitoring:
                          description: |-
                            Set this flag to enable CloudWatch monitoring. Deafult false (bool)
                            Enable monitoring for droplet
                          type: boolean
                        privateNetworking:
                          description: |-
                            Enable private networking for droplet. Default false (bool)
                            Enable private networking for droplet
                          type: boolean
                        region:
                          description: |-
                            AWS region. (string)
                            Digital Ocean region
                          type: string
                        size:
                          description: |-
                            Size for Azure Virtual Machine. Default Standard_D2_v2 (string)
                            Digital Ocean size
                          type: string
                        sshKeyFingerprintSecretRef:
                          description: |-
                            SSH key fingerprint (string)
                            SSH key fingerprint
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        sshKeyPath:
                          description: |-
                            SSH private key path (string)
                            SSH private key path
                          type: string
                        sshPort:
                          description: |-
                            SSH port. Default 22 (string)
                            SSH port
                          type: string
                        sshUser:
                          description: |-
                            Set the name of the ssh user (string)
                            SSH username
                          type: string
                        tags:
                          description: |-
                            AWS Tags (e.g. key1,value1,key2,value2) (string)
                            Comma-separated list of tags to apply to the Droplet
                          type: string
                        userdata:
                          description: |-
                            Path to file with cloud-init user data (string)
                            Path to file with cloud-init user-data
                          type: string
                      type: object
                    type: array
                  driverId:
                    description: The node driver id used by the node template. It's
                      required if the node driver isn't built in Rancher (string)
                    type: string
                  driverIdRef:
                    description: Reference to a NodeDriver in rancher to populate
                      driverId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  driverIdSelector:
                    description: Selector for a NodeDriver in rancher to populate
                      driverId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  engineEnv:
                    additionalProperties:
                      type: string
                    description: Engine environment for the node template (string)
                    type: object
                    x-kubernetes-map-type: granular
                  engineInsecureRegistry:
                    description: Insecure registry for the node template (list)
                    items:
                      type: string
                    type: array
                  engineInstallUrl:
                    description: Docker engine install URL for the node template.
                      Available install docker versions at https://github.com/rancher/install-docker
                      (string)
                    type: string
                  engineLabel:
                    additionalProperties:
                      type: string
                    description: Engine label for the node template (string)
                    type: object
                    x-kubernetes-map-type: granular
                  engineOpt:
                    additionalProperties:
                      type: string
                    description: Engine options for the node template (map)
                    type: object
                    x-kubernetes-map-type: granular
                  engineRegistryMirror:
                    description: Engine registry mirror for the node template (list)
                    items:
                      type: string
                    type: array
                  engineStorageDriver:
                    description: Engine storage driver for the node template (string)
                    type: string
                  harvesterConfig:
                    description: Harvester config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        cpuCount:
                          description: |-
                            CPU count, Default 2 (string)
                            CPU count
                          type: string
                        diskBus:
                          description: |-
                            (Deprecated) Use disk_info instead
                            Disk bus
                          type: string
                        diskInfo:
                          description: |-
                            A JSON string specifying info for the disks e.g. {\"disks\":[{\"imageName\":\"harvester-public/image-57hzg\",\"bootOrder\":1,\"size\":40},{\"storageClassName\":\"node-driver-test\",\"bootOrder\":2,\"size\":1}]} (string)
                            A JSON string specifying info for the disks e.g. `{"disks":[{"imageName":"harvester-public/image-57hzg","bootOrder":1,"size":40},{"storageClassName":"node-driver-test","bootOrder":2,"size":1}]}`
                          type: string
                        diskSize:
                          description: |-
                            Disk size if using managed disk. For Rancher v2.3.x and above. Default 30 (string)
                            Disk size (in GiB)
                          type: string
                        imageName:
                          description: |-
                            (Deprecated) Use disk_info instead
                            Image name
                          type: string
                        memorySize:
                          description: |-
                            Memory size (in GiB), Default 4 (string)
                            Memory size (in GiB)
                          type: string
                        networkData:
                          description: |-
                            NetworkData content of cloud-init, base64 is supported (string)
                            NetworkData content of cloud-init, base64 is supported
                          type: string
                        networkInfo:
                          description: |-
                            A JSON string specifying info for the networks e.g. {\"interfaces\":[{\"networkName\":\"harvester-public/vlan1\"},{\"networkName\":\"harvester-public/vlan2\"}]} (string)
                            A JSON string specifying info for the networks e.g. `{"interfaces":[{"networkName":"harvester-public/vlan1"},{"networkName":"harvester-public/vlan2"}]}`
                          type: string
                        networkModel:
                          description: |-
                            (Deprecated) Use network_info instead
                            Network model
                          type: string
                        networkName:
                          description: |-
                            (Deprecated) Use network_info instead
                            Network name
                          type: string
                        sshPasswordSecretRef:
                          description: |-
                            SSH password (string)
                            SSH password
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        sshUser:
                          description: |-
                            Set the name of the ssh user (string)
                            SSH username
                          type: string
                        userData:
                          description: |-
                            UserData content of cloud-init, base64 is supported. If the image does not contain the qemu-guest-agent package, you must install and start qemu-guest-agent using userdata (string)
                            UserData content of cloud-init, base64 is supported. If the image does not contain the qemu-guest-agent package, you must install and start qemu-guest-agent using userdata
                          type: string
                        vmAffinity:
                          description: |-
                            Virtual machine affinity, only base64 format is supported. For Rancher v2.6.7 and above (string)
                            VM affinity, base64 is supported
                          type: string
                        vmNamespace:
                          description: |-
                            Virtual machine namespace e.g. default (string)
                            Virtual machine namespace
                          type: string
                      type: object
                    type: array
                  hetznerConfig:
                    description: Hetzner config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        apiTokenSecretRef:
                          description: |-
                            Hetzner Cloud project API token (string)
                            Hetzner Cloud project API token
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        image:
                          description: |-
                            Azure virtual machine OS image. Default canonical:ubuntu-24_04-lts:server-gen1:latest (string)
                            Hetzner Cloud server image
                          type: string
                        networks:
                          description: |-
                            Comma-separated list of network IDs or names which should be attached to the server private network interface (string)
                            Comma-separated list of network IDs or names which should be attached to the server private network interface
                          type: string
                        serverLabels:
                          additionalProperties:
                            type: string
                          description: |-
                            Map of the labels which will be assigned to the server. This argument is only available on Hetzner Docker Node Driver:v3.6.0 and above (map)
                            Map of the labels which will be assigned to the server
                          type: object
                          x-kubernetes-map-type: granular
                        serverLocation:
                          description: |-
                            Hetzner Cloud datacenter. Default nbg1 (string)
                            Hetzner Cloud datacenter
                          type: string
                        serverType:
                          description: |-
                            Hetzner Cloud server type. Default cx11 (string)
                            Hetzner Cloud server type
                          type: string
                        usePrivateNetwork:
                          description: |-
                            Use private network. Default false (bool)
                            Use private network
                          type: boolean
                        userdata:
                          description: |-
                            Path to file with cloud-init user data (string)
                            Path to file with cloud-init user-data
                          type: string
                        volumes:
                          description: |-
                            Comma-separated list of volume IDs or names which should be attached to the server (string)
                            Comma-separated list of volume IDs or names which should be attached to the server
                          type: string
                      required:
                      - apiTokenSecretRef
                      type: object
                    type: array
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      Labels for Node Template object (map)
                      Labels of the resource
                    type: object
                    x-kubernetes-map-type: granular
                  linodeConfig:
                    description: Linode config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        authorizedUsers:
                          description: |-
                            Linode user accounts (seperated by commas) whose Linode SSH keys will be permitted root access to the created node. (string)
                            Linode user accounts (seperated by commas) whose Linode SSH keys will be permitted root access to the created node
                          type: string
                        createPrivateIp:
                          description: |-
                            Create private IP for the instance. Default false (bool)
                            Create private IP for the instance
                          type: boolean
                        dockerPort:
                          description: |-
                            Port number for Docker engine. Default 2376 (string)
                            Docker Port
                          type: string
                        image:
                          description: |-
                            Azure virtual machine OS image. Default canonical:ubuntu-24_04-lts:server-gen1:latest (string)
                            Specifies the Linode Instance image which determines the OS distribution and base files
                          type: string
                        instanceType:
                          description: |-
                            AWS instance type. Default t2.micro (string)
                            Specifies the Linode Instance type which determines CPU, memory, disk size, etc.
                          type: string
                        label:
                          description: |-
                            Linode Instance Label. (string)
                            Linode Instance Label
                          type: string
                        region:
                          description: |-
                            AWS region. (string)
                            Specifies the region (location) of the Linode instance
                          type: string
                        rootPassSecretRef:
                          description: |-
                            Root Password (string)
                            Root Password
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        sshPort:
                          description: |-
                            SSH port. Default 22 (string)
                            Linode Instance SSH Port
                          type: string
                        sshUser:
                          description: |-
                            Set the name of the ssh user (string)
                            Specifies the user as which docker-machine should log in to the Linode instance to install Docker.
                          type: string
                        stackscript:
                          description: |-
                            Specifies the Linode StackScript to use to create the instance. (string)
                            Specifies the Linode StackScript to use to create the instance
                          type: string
                        stackscriptData:
                          description: |-
                            A JSON string specifying data for the selected StackScript. (string)
                            A JSON string specifying data for the selected StackScript
                          type: string
                        swapSize:
                          description: |-
                            Linode Instance Swap Size (MB). Default 512 (string)
                            Linode Instance Swap Size (MB)
                          type: string
                        tags:
                          description: |-
                            AWS Tags (e.g. key1,value1,key2,value2) (string)
                            A comma separated list of tags to apply to the the Linode resource
                          type: string
                        tokenSecretRef:
                          description: |-
                            Linode API token. Mandatory on Rancher v2.0.x and v2.1.x. Use rancher2_cloud_credential from Rancher v2.2.x (string)
                            Linode API Token
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        uaPrefix:
                          description: |-
                            Prefix the User-Agent in Linode API calls with some 'product/version' (string)
                            Prefix the User-Agent in Linode API calls with some 'product/version'
                          type: string
                      type: object
                    type: array
                  name:
                    description: The name of the Node Template (string)
                    type: string
                  nodeTaints:
                    description: Node taints. For Rancher v2.3.3 and above (List)
                    items:
                      properties:
                        effect:
                          description: 'Taint effect. Supported values : "NoExecute"
                            | "NoSchedule" | "PreferNoSchedule" (string)'
                          type: string
                        key:
                          description: Taint key (string)
                          type: string
                        timeAdded:
                          description: Taint time added (string)
                          type: string
                        value:
                          description: Taint value (string)
                          type: string
                      type: object
                    type: array
                  opennebulaConfig:
                    description: Opennebula config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        b2dSize:
                          description: Size of the Volatile disk in MB - only for
                            b2d (string)
                          type: string
                        cpu:
                          description: CPU value for the VM (string)
                          type: string
                        devPrefix:
                          description: 'Dev prefix to use for the images. E.g.: ''vd'',
                            ''sd'', ''hd'' (string)'
                          type: string
                        disableVnc:
                          description: VNC is enabled by default. Disable it with
                            this flag (bool)
                          type: boolean
                        diskResize:
                          description: Size of the disk for the VM in MB (string)
                          type: string
                        imageId:
                          description: Image ID to use as the VM OS. Conflicts with
                            image_name (string)
                          type: string
                        imageName:
                          description: (Deprecated) Use disk_info instead
                          type: string
                        imageOwner:
                          description: Owner of the image to use as the VM OS (string)
                          type: string
                        memory:
                          description: Size of the memory for the VM in MB (string)
                          type: string
                        networkId:
                          description: Opennebula network ID to connect the machine
                            to. Conflicts with network_name (string)
                          type: string
                        networkName:
                          description: (Deprecated) Use network_info instead
                          type: string
                        networkOwner:
                          description: Opennebula user ID of the Network to connect
                            the machine to (string)
                          type: string
                        passwordSecretRef:
                          description: Set the password for the XML-RPC API authentication
                            (string)
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        sshUser:
                          description: Set the name of the ssh user (string)
                          type: string
                        templateId:
                          description: Opennebula template ID to use. Conflicts with
                            template_name (string)
                          type: string
                        templateName:
                          description: Name of the Opennbula template to use. Conflicts
                            with template_id (string)
                          type: string
                        user:
                          description: Set the user for the XML-RPC API authentication
                            (string)
                          type: string
                        vcpu:
                          description: VCPUs for the VM (string)
                          type: string
                        xmlRpcUrl:
                          description: Set the url for the Opennebula XML-RPC API
                            (string)
                          type: string
                      required:
                      - passwordSecretRef
                      type: object
                    type: array
                  openstackConfig:
                    description: Openstack config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        activeTimeout:
                          description: OpenStack active timeout Default 200 (string)
                          type: string
                        applicationCredentialId:
                          description: OpenStack application credential id. Conflicts
                            with application_credential_name (string)
                          type: string
                        applicationCredentialName:
                          description: OpenStack application credential name. Conflicts
                            with application_credential_id (string)
                          type: string
                        applicationCredentialSecretSecretRef:
                          description: OpenStack application credential secret (string)
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        authUrl:
                          description: OpenStack authentication URL (string)
                          type: string
                        availabilityZone:
                          description: OpenStack availability zone (string)
                          type: string
                        bootFromVolume:
                          description: Enable booting from volume. Default is false
                            (bool)
                          type: boolean
                        cacert:
                          description: CA certificate bundle to verify against (string)
                          type: string
                        configDrive:
                          description: Enables the OpenStack config drive for the
                            instance. Default false (bool)
                          type: boolean
                        domainId:
                          description: OpenStack domain ID. Identity v3 only. Conflicts
                            with domain_name (string)
                          type: string
                        domainName:
                          description: OpenStack domain name. Identity v3 only. Conflicts
                            with domain_id (string)
                          type: string
                        endpointType:
                          description: OpenStack endpoint type. adminURL, internalURL
                            or publicURL (string)
                          type: string
                        flavorId:
                          description: OpenStack flavor id to use for the instance.
                            Conflicts with flavor_name (string)
                          type: string
                        flavorName:
                          description: OpenStack flavor name to use for the instance.
                            Conflicts with flavor_id (string)
                          type: string
                        floatingIpPool:
                          description: OpenStack floating IP pool to get an IP from
                            to assign to the instance (string)
                          type: string
                        imageId:
                          description: Image ID to use as the VM OS. Conflicts with
                            image_name (string)
                          type: string
                        imageName:
                          description: (Deprecated) Use disk_info instead
                          type: string
                        insecure:
                          description: Disable TLS credential checking. Default false
                            (bool)
                          type: boolean
                        ipVersion:
                          description: OpenStack version of IP address assigned for
                            the machine Default 4 (string)
                          type: string
                        keypairName:
                          description: OpenStack keypair to use to SSH to the instance
                            (string)
                          type: string
                        netId:
                          description: OpenStack network id the machine will be connected
                            on. Conflicts with net_name (string)
                          type: string
                        netName:
                          description: OpenStack network name the machine will be
                            connected on. Conflicts with net_id (string)
                          type: string
                        novaNetwork:
                          description: Use the nova networking services instead of
                            neutron (string)
                          type: boolean
                        passwordSecretRef:
                          description: Set the password for the XML-RPC API authentication
                            (string)
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        privateKeyFileSecretRef:
                          description: Private key content to use for SSH (string)
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        region:
                          description: AWS region. (string)
                          type: string
                        secGroups:
                          description: OpenStack comma separated security groups for
                            the machine (string)
                          type: string
                        sshPort:
                          description: SSH port. Default 22 (string)
                          type: string
                        sshUser:
                          description: Set the name of the ssh user (string)
                          type: string
                        tenantId:
                          description: OpenStack tenant id. Conflicts with tenant_name
                            (string)
                          type: string
                        tenantName:
                          description: OpenStack tenant name. Conflicts with tenant_id
                            (string)
                          type: string
                        userDataFile:
                          description: File containing an openstack userdata script
                            (string)
                          type: string
                        username:
                          description: OpenStack username (string)
                          type: string
                        volumeDevicePath:
                          description: OpenStack volume device path (attaching). Applicable
                            only when boot_from_volume is true. Omit for auto /dev/vdb.
                            (string)
                          type: string
                        volumeId:
                          description: OpenStack volume id of existing volume. Applicable
                            only when boot_from_volume is true (string)
                          type: string
                        volumeName:
                          description: OpenStack volume name of existing volume. Applicable
                            only when boot_from_volume is true (string)
                          type: string
                        volumeSize:
                          description: OpenStack volume size (GiB). Required when
                            boot_from_volume is true (string)
                          type: string
                        volumeType:
                          description: Amazon EBS volume type. Default gp2 (string)
                          type: string
                      type: object
                    type: array
                  outscaleConfig:
                    description: Outscale config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        accessKeySecretRef:
                          description: |-
                            AWS access key. Required on Rancher v2.0.x and v2.1.x. Use rancher2_cloud_credential from Rancher v2.2.x (string)
                            Outscale Access Key
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        extraTagsAll:
                          description: |-
                            Extra tags for all created resources (e.g. key1=value1,key2=value2) (list)
                            Extra tags for all created resources (e.g. key1=value1,key2=value2)
                          items:
                            type: string
                          type: array
                        extraTagsInstances:
                          description: |-
                            Extra tags only for instances (e.g. key1=value1,key2=value2) (list)
                            Extra tags only for instances (e.g. key1=value1,key2=value2)
                          items:
                            type: string
                          type: array
                        instanceType:
                          description: |-
                            AWS instance type. Default t2.micro (string)
                            Outscale VM type
                          type: string
                        region:
                          description: |-
                            AWS region. (string)
                            Outscale Region
                          type: string
                        rootDiskIops:
                          description: |-
                            Iops for io1 Root Disk. From 1 to 13000.
                            Iops for io1 Root Disk. From 1 to 13000.
                          type: number
                        rootDiskSize:
                          description: |-
                            Size of the Root Disk (in GB). From 1 to 14901.
                            Size of the Root Disk (in GB). From 1 to 14901.
                          type: number
                        rootDiskType:
                          description: |-
                            Type of the Root Disk. Possible values are :'standard', 'gp2' or 'io1'.
                            Type of the Root Disk. Possible values are :'standard', 'gp2' or 'io1'.
                          type: string
                        secretKeySecretRef:
                          description: |-
                            AWS secret key. Required on Rancher v2.0.x and v2.1.x. Use rancher2_cloud_credential from Rancher v2.2.x (string)
                            Outscale Secret Key
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        securityGroupIds:
                          description: |-
                            Ids of user defined Security Groups to add to the machine. (list)
                            Ids of user defined Security Groups to add to the machine
                          items:
                            type: string
                          type: array
                        sourceOmi:
                          description: |-
                            Outscale Machine Image to use as bootstrap for the VM. Default ami-2cf1fa3e (string)
                            Outscale Machine Image to use as bootstrap for the VM
                          type: string
                      required:
                      - accessKeySecretRef
                      - secretKeySecretRef
                      type: object
                    type: array
                  useInternalIpAddress:
                    description: Engine storage driver for the node template (bool)
                    type: boolean
                  vsphereConfig:
                    description: vSphere config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        boot2dockerUrl:
                          description: |-
                            vSphere URL for boot2docker iso image. Default https://releases.rancher.com/os/latest/rancheros-vmware.iso (string)
                            vSphere URL for boot2docker image
                          type: string
                        cfgparam:
                          description: |-
                            vSphere vm configuration parameters (used for guestinfo) (list)
                            vSphere vm configuration parameters (used for guestinfo)
                          items:
                            type: string
                          type: array
                        cloneFrom:
                          description: |-
                            If you choose creation type vm (clone vm) a name of what vm you want to clone is required. From Rancher v2.3.3 (string)
                            If you choose creation type clone a name of what you want to clone is required
                          type: string
                        cloudConfig:
                          description: |-
                            Cloud Config YAML content to inject as user-data. From Rancher v2.3.3 (string)
                            Filepath to a cloud-config yaml file to put into the ISO user-data
                          type: string
                        cloudinit:
                          description: |-
                            vSphere cloud-init file or url to set in the guestinfo (string)
                            vSphere cloud-init filepath or url to add to guestinfo
                          type: string
                        contentLibrary:
                          description: |-
                            If you choose to clone from a content library template specify the name of the library. From Rancher v2.3.3 (string)
                            If you choose to clone from a content library template specify the name of the library
                          type: string
                        cpuCount:
                          description: |-
                            CPU count, Default 2 (string)
                            vSphere CPU number for docker VM
                          type: string
                        creationType:
                          description: |-
                            Creation type when creating a new virtual machine. Supported values: vm, template, library, legacy. Default legacy. From Rancher v2.3.3 (string)
                            Creation type when creating a new virtual machine. Supported values: vm, template, library, legacy
                          type: string
                        customAttributes:
                          description: |-
                            vSphere custom attributes, format key/value e.g. 200=my custom value. From Rancher v2.3.3 (List)
                            vSphere custom attributes, format key/value e.g. '200=my custom value'
                          items:
                            type: string
                          type: array
                        datacenter:
                          description: |-
                            vSphere datacenter for docker VM (string)
                            vSphere datacenter for virtual machine
                          type: string
                        datastore:
                          description: |-
                            vSphere datastore for docker VM (string)
                            vSphere datastore for virtual machine
                          type: string
                        datastoreCluster:
                          description: |-
                            vSphere datastore cluster for virtual machine. From Rancher v2.3.3 (string)
                            vSphere datastore cluster for virtual machine
                          type: string
                        diskSize:
                          description: |-
                            Disk size if using managed disk. For Rancher v2.3.x and above. Default 30 (string)
                            vSphere size of disk for docker VM (in MB)
                          type: string
                        folder:
                          description: |-
                            vSphere folder for the docker VM. This folder must already exist in the datacenter (string)
                            vSphere folder for the docker VM. This folder must already exist in the datacenter
                          type: string
                        gracefulShutdownTimeout:
                          description: Duration in seconds before the graceful shutdown
                            of the VM times out and the VM is destroyed. A force destroy
                            will be performed when the value is zero
                          type: string
                        hostsystem:
                          description: |-
                            vSphere compute resource where the docker VM will be instantiated. This can be omitted if using a cluster with DRS (string)
                            vSphere compute resource where the docker VM will be instantiated. This can be omitted if using a cluster with DRS
                          type: string
                        memorySize:
                          description: |-
                            Memory size (in GiB), Default 4 (string)
                            vSphere size of memory for docker VM (in MB)
                          type: string
                        network:
                          description: |-
                            vSphere network where the docker VM will be attached (list)
                            vSphere network where the virtual machine will be attached
                          items:
                            type: string
                          type: array
                        passwordSecretRef:
                          description: |-
                            Set the password for the XML-RPC API authentication (string)
                            vSphere password
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        pool:
                          description: |-
                            vSphere resource pool for docker VM (string)
                            vSphere resource pool for docker VM
                          type: string
                        sshPasswordSecretRef:
                          description: |-
                            SSH password (string)
                            If using a non-B2D image you can specify the ssh password
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        sshPort:
                          description: |-
                            SSH port. Default 22 (string)
                            If using a non-B2D image you can specify the ssh port
                          type: string
                        sshUser:
                          description: |-
                            Set the name of the ssh user (string)
                            If using a non-B2D image you can specify the ssh user
                          type: string
                        sshUserGroup:
                          description: |-
                            If using a non-B2D image the uploaded keys will need chown'ed. Default staff. From Rancher v2.3.3 (string)
                            If using a non-B2D image the uploaded keys will need chown'ed, defaults to staff e.g. docker:staff
                          type: string
                        tags:
                          description: |-
                            AWS Tags (e.g. key1,value1,key2,value2) (string)
                            vSphere tags id e.g. urn:xxx
                          items:
                            type: string
                          type: array
                        username:
                          description: |-
                            OpenStack username (string)
                            vSphere username
                          type: string
                        vappIpAllocationPolicy:
                          description: |-
                            vSphere vApp IP allocation policy. Supported values are: dhcp, fixed, transient and fixedAllocated (string)
                            vSphere vApp IP allocation policy. Supported values are: dhcp, fixed, transient and fixedAllocated
                          type: string
                        vappIpProtocol:
                          description: |-
                            vSphere vApp IP protocol for this deployment. Supported values are: IPv4 and IPv6 (string)
                            vSphere vApp IP protocol for this deployment. Supported values are: IPv4 and IPv6
                          type: string
                        vappProperty:
                          description: |-
                            vSphere vApp properties (list)
                            vSphere vApp properties
                          items:
                            type: string
                          type: array
                        vappTransport:
                          description: |-
                            vSphere OVF environment transports to use for properties. Supported values are: iso and com.vmware.guestInfo (string)
                            vSphere OVF environment transports to use for properties. Supported values are: iso and com.vmware.guestInfo
                          type: string
                        vcenter:
                          description: |-
                            vSphere IP/hostname for vCenter. Mandatory on Rancher v2.0.x and v2.1.x. Use rancher2_cloud_credential from Rancher v2.2.x (string)
                            vSphere IP/hostname for vCenter
                          type: string
                        vcenterPort:
                          description: |-
                            vSphere Port for vCenter. Mandatory on Rancher v2.0.x and v2.1.x. Use rancher2_cloud_credential from Rancher v2.2.x. Default 443 (string)
                            vSphere Port for vCenter
                          type: string
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: |-
                  PublishConnectionDetailsTo specifies the connection secret config which
                  contains a name, metadata and a reference to secret store config to
                  which any connection details for this managed resource should be written.
                  Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: |-
                      SecretStoreConfigRef specifies which secret store config should be used
                      for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          Annotations are the annotations to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.annotations".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          Labels are the labels/tags to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      type:
                        description: |-
                          Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                  This field is planned to be replaced in a future release in favor of
                  PublishConnectionDetailsTo. Currently, both could be set independently
                  and connection details would be published to both without affecting
                  each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: NodeTemplateStatus defines the observed state of NodeTemplate.
            properties:
              atProvider:
                properties:
                  amazonec2Config:
                    description: AWS config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        ami:
                          description: |-
                            AWS machine image (string)
                            AWS machine image
                          type: string
                        blockDurationMinutes:
                          description: |-
                            AWS spot instance duration in minutes (60, 120, 180, 240, 300, or 360). Default 0 (string)
                            AWS spot instance duration in minutes (60, 120, 180, 240, 300, or 360)
                          type: string
                        deviceName:
                          description: |-
                            AWS root device name. Default /dev/sda1 (string)
                            AWS root device name
                          type: string
                        encryptEbsVolume:
                          description: |-
                            Encrypt EBS volume. Default false (bool)
                            Encrypt EBS volume
                          type: boolean
                        endpoint:
                          description: |-
                            Optional endpoint URL (hostname only or fully qualified URI) (string)
                            Optional endpoint URL (hostname only or fully qualified URI)
                          type: string
                        httpEndpoint:
                          description: |-
                            Enables or disables the HTTP metadata endpoint on your instances (string)
                            Enables or disables the HTTP metadata endpoint on your instances
                          type: string
                        httpTokens:
                          description: |-
                            The state of token usage for your instance metadata requests (string)
                            The state of token usage for your instance metadata requests
                          type: string
                        iamInstanceProfile:
                          description: |-
                            AWS IAM Instance Profile (string)
                            AWS IAM Instance Profile
                          type: string
                        insecureTransport:
                          description: |-
                            Disable SSL when sending requests (bool)
                            Disable SSL when sending requests
                          type: boolean
                        instanceType:
                          description: |-
                            AWS instance type. Default t2.micro (string)
                            AWS instance type
                          type: string
                        kmsKey:
                          description: |-
                            Custom KMS key ID using the AWS Managed CMK (string)
                            Custom KMS key ID using the AWS Managed CMK
                          type: string
                        monitoring:
                          description: |-
                            Set this flag to enable CloudWatch monitoring. Deafult false (bool)
                            Set this flag to enable CloudWatch monitoring
                          type: boolean
                        openPort:
                          description: |-
                            Make the specified port number accessible from the Internet. (list)
                            Make the specified port number accessible from the Internet
                          items:
                            type: string
                          type: array
                        privateAddressOnly:
                          description: |-
                            Only use a private IP address. Default false (bool)
                            Only use a private IP address
                          type: boolean
                        region:
                          description: |-
                            AWS region. (string)
                            AWS Region
                          type: string
                        requestSpotInstance:
                          description: |-
                            Set this flag to request spot instance. Default false (bool)
                            Set this flag to request spot instance
                          type: boolean
                        retries:
                          description: |-
                            Set retry count for recoverable failures (use -1 to disable). Default 5 (string)
                            Set retry count for recoverable failures (use -1 to disable)
                          type: string
                        rootSize:
                          description: |-
                            AWS root disk size (in GB). Default 16 (string)
                            AWS root disk size (in GB)
                          type: string
                        securityGroup:
                          description: |-
                            AWS VPC security group. (list)
                            AWS VPC security group
                          items:
                            type: string
                          type: array
                        securityGroupReadonly:
                          description: |-
                            Skip adding default rules to security groups (bool)
                            Skip adding default rules to security groups
                          type: boolean
                        spotPrice:
                          description: |-
                            AWS spot instance bid price (in dollar). Default 0.50 (string)
                            AWS spot instance bid price (in dollar)
                          type: string
                        sshKeypath:
                          description: SSH Key for Instance
                          type: string
                        sshUser:
                          description: |-
                            Set the name of the ssh user (string)
                            Set the name of the ssh user
                          type: string
                        subnetId:
                          description: |-
                            AWS VPC subnet id (string)
                            AWS VPC subnet id
                          type: string
                        tags:
                          description: |-
                            AWS Tags (e.g. key1,value1,key2,value2) (string)
                            AWS Tags (e.g. key1,value1,key2,value2)
                          type: string
                        useEbsOptimizedInstance:
                          description: |-
                            Create an EBS optimized instance. Default false (bool)
                            Create an EBS optimized instance
                          type: boolean
                        usePrivateAddress:
                          description: |-
                            Force the usage of private IP address. Default false (bool)
                            Force the usage of private IP address
                          type: boolean
                        userdata:
                          description: |-
                            Path to file with cloud-init user data (string)
                            Path to file with cloud-init user data
                          type: string
                        volumeType:
                          description: |-
                            Amazon EBS volume type. Default gp2 (string)
                            Amazon EBS volume type
                          type: string
                        vpcId:
                          description: |-
                            AWS VPC id. (string)
                            AWS VPC id
                          type: string
                        zone:
                          description: |-
                            AWS zone for instance (i.e. a,b,c,d,e) (string)
                            AWS zone for instance (i.e. a,b,c,d,e)
                          type: string
                      type: object
                    type: array
                  annotations:
                    additionalProperties:
                      type: string
                    description: |-
                      Annotations for Node Template object (map)
                      Annotations of the resource
                    type: object
                    x-kubernetes-map-type: granular
                  azureConfig:
                    description: Azure config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        acceleratedNetworking:
                          description: Enable Accelerated Networking when creating
                            an Azure Network Interface
                          type: boolean
                        availabilitySet:
                          description: |-
                            Azure Availability Set to place the virtual machine into. Default docker-machine (string)
                            Azure Availability Set to place the virtual machine into
                          type: string
                        availabilityZone:
                          description: |-
                            OpenStack availability zone (string)
                            The Azure Availability Zone the VM should be created in
                          type: string
                        customData:
                          description: |-
                            Path to file with custom-data (string)
                            Path to file with custom-data
                          type: string
                        diskSize:
                          description: |-
                            Disk size if using managed disk. For Rancher v2.3.x and above. Default 30 (string)
                            Disk size if using managed disk
                          type: string
                        dns:
                          description: |-
                            A unique DNS label for the public IP adddress (string)
                            A unique DNS label for the public IP adddress
                          type: string
                        dockerPort:
                          description: |-
                            Port number for Docker engine. Default 2376 (string)
                            Port number for Docker engine
                          type: string
                        environment:
                          description: |-
                            Azure environment (e.g. AzurePublicCloud, AzureChinaCloud). Default AzurePublicCloud (string)
                            Azure environment (e.g. AzurePublicCloud, AzureChinaCloud)
                          type: string
                        faultDomainCount:
                          description: |-
                            Fault domain count to use for availability set. Default 3 (string)
                            Fault domain count to use for availability set
                          type: string
                        image:
                          description: |-
                            Azure virtual machine OS image. Default canonical:ubuntu-24_04-lts:server-gen1:latest (string)
                            Azure virtual machine OS image
                          type: string
                        location:
                          description: |-
                            Azure region to create the virtual machine. Default westus (string)
                            Azure region to create the virtual machine
                          type: string
                        managedDisks:
                          description: |-
                            Configures VM and availability set for managed disks. For Rancher v2.3.x and above. Default false (bool)
                            Configures VM and availability set for managed disks
                          type: boolean
                        noPublicIp:
                          description: |-
                            Do not create a public IP address for the machine. Default false (bool)
                            Do not create a public IP address for the machine
                          type: boolean
                        nsg:
                          description: |-
                            Azure Network Security Group to assign this node to (accepts either a name or resource ID, default is to create a new NSG for each machine). Default docker-machine-nsg (string)
                            Azure Network Security Group to assign this node to (accepts either a name or resource ID, default is to create a new NSG for each machine)
                          type: string
                        openPort:
                          description: |-
                            Make the specified port number accessible from the Internet. (list)
                            Make the specified port number accessible from the Internet
                          items:
                            type: string
                          type: array
                        plan:
                          description: |-
                            Azure marketplace purchase plan for Azure Virtual Machine. Format is <publisher>:<product>:<plan>. For Rancher v2.6.3 and above. (string)
                            Purchase plan for Azure Virtual Machine (in <publisher>:<product>:<plan> format)
                          type: string
                        privateIpAddress:
                          description: |-
                            Specify a static private IP address for the machine. (string)
                            Specify a static private IP address for the machine
                          type: string
                        resourceGroup:
                          description: |-
                            Azure Resource Group name (will be created if missing). Default docker-machine (string)
                            Azure Resource Group name (will be created if missing)
                          type: string
                        size:
                          description: |-
                            Size for Azure Virtual Machine. Default Standard_D2_v2 (string)
                            Size for Azure Virtual Machine
                          type: string
                        sshUser:
                          description: |-
                            Set the name of the ssh user (string)
                            Username for SSH login
                          type: string
                        staticPublicIp:
                          description: |-
                            Assign a static public IP address to the machine. Default false (bool)
                            Assign a static public IP address to the machine
                          type: boolean
                        storageType:
                          description: |-
                            Type of Storage Account to host the OS Disk for the machine. Default Standard_LRS (string)
                            Type of Storage Account to host the OS Disk for the machine
                          type: string
                        subnet:
                          description: |-
                            Azure Subnet Name to be used within the Virtual Network. Default docker-machine (string)
                            Azure Subnet Name to be used within the Virtual Network
                          type: string
                        subnetPrefix:
                          description: |-
                            Private CIDR block to be used for the new subnet, should comply RFC 1918. Default 192.168.0.0/16 (string)
                            Private CIDR block to be used for the new subnet, should comply RFC 1918
                          type: string
                        tags:
                          description: |-
                            AWS Tags (e.g. key1,value1,key2,value2) (string)
                            Tags to be applied to the Azure VM instance (e.g. key1,value1,key2,value2)
                          type: string
                        updateDomainCount:
                          description: |-
                            Update domain count to use for availability set. Default 5 (string)
                            Update domain count to use for availability set
                          type: string
                        usePrivateIp:
                          description: |-
                            Use private IP address of the machine to connect. Default false (bool)
                            Use private IP address of the machine to connect
                          type: boolean
                        usePublicIpStandardSku:
                          description: Use the Standard SKU when creating a public
                            IP for an Azure VM
                          type: boolean
                        vnet:
                          description: |-
                            Azure Virtual Network name to connect the virtual machine (in [resourcegroup:]name format). Default docker-machine-vnet (string)
                            Azure Virtual Network name to connect the virtual machine (in [resourcegroup:]name format)
                          type: string
                      type: object
                    type: array
                  cloudCredentialId:
                    description: Cloud credential ID for the Node Template. Required
                      from Rancher v2.2.x (string)
                    type: string
                  description:
                    description: Description for the Node Template (string)
                    type: string
                  digitaloceanConfig:
                    description: Digitalocean config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        backups:
                          description: |-
                            Enable backups for droplet. Default false (bool)
                            Enable backups for droplet
                          type: boolean
                        image:
                          description: |-
                            Azure virtual machine OS image. Default canonical:ubuntu-24_04-lts:server-gen1:latest (string)
                            Digital Ocean Image
                          type: string
                        ipv6:
                          description: |-
                            Enable ipv6 for droplet. Default false (bool)
                            Enable ipv6 for droplet
                          type: boolean
                        monitoring:
                          description: |-
                            Set this flag to enable CloudWatch monitoring. Deafult false (bool)
                            Enable monitoring for droplet
                          type: boolean
                        privateNetworking:
                          description: |-
                            Enable private networking for droplet. Default false (bool)
                            Enable private networking for droplet
                          type: boolean
                        region:
                          description: |-
                            AWS region. (string)
                            Digital Ocean region
                          type: string
                        size:
                          description: |-
                            Size for Azure Virtual Machine. Default Standard_D2_v2 (string)
                            Digital Ocean size
                          type: string
                        sshKeyPath:
                          description: |-
                            SSH private key path (string)
                            SSH private key path
                          type: string
                        sshPort:
                          description: |-
                            SSH port. Default 22 (string)
                            SSH port
                          type: string
                        sshUser:
                          description: |-
                            Set the name of the ssh user (string)
                            SSH username
                          type: string
                        tags:
                          description: |-
                            AWS Tags (e.g. key1,value1,key2,value2) (string)
                            Comma-separated list of tags to apply to the Droplet
                          type: string
                        userdata:
                          description: |-
                            Path to file with cloud-init user data (string)
                            Path to file with cloud-init user-data
                          type: string
                      type: object
                    type: array
                  driver:
                    description: (Computed) The driver of the node template (string)
                    type: string
                  driverId:
                    description: The node driver id used by the node template. It's
                      required if the node driver isn't built in Rancher (string)
                    type: string
                  engineEnv:
                    additionalProperties:
                      type: string
                    description: Engine environment for the node template (string)
                    type: object
                    x-kubernetes-map-type: granular
                  engineInsecureRegistry:
                    description: Insecure registry for the node template (list)
                    items:
                      type: string
                    type: array
                  engineInstallUrl:
                    description: Docker engine install URL for the node template.
                      Available install docker versions at https://github.com/rancher/install-docker
                      (string)
                    type: string
                  engineLabel:
                    additionalProperties:
                      type: string
                    description: Engine label for the node template (string)
                    type: object
                    x-kubernetes-map-type: granular
                  engineOpt:
                    additionalProperties:
                      type: string
                    description: Engine options for the node template (map)
                    type: object
                    x-kubernetes-map-type: granular
                  engineRegistryMirror:
                    description: Engine registry mirror for the node template (list)
                    items:
                      type: string
                    type: array
                  engineStorageDriver:
                    description: Engine storage driver for the node template (string)
                    type: string
                  harvesterConfig:
                    description: Harvester config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        cpuCount:
                          description: |-
                            CPU count, Default 2 (string)
                            CPU count
                          type: string
                        diskBus:
                          description: |-
                            (Deprecated) Use disk_info instead
                            Disk bus
                          type: string
                        diskInfo:
                          description: |-
                            A JSON string specifying info for the disks e.g. {\"disks\":[{\"imageName\":\"harvester-public/image-57hzg\",\"bootOrder\":1,\"size\":40},{\"storageClassName\":\"node-driver-test\",\"bootOrder\":2,\"size\":1}]} (string)
                            A JSON string specifying info for the disks e.g. `{"disks":[{"imageName":"harvester-public/image-57hzg","bootOrder":1,"size":40},{"storageClassName":"node-driver-test","bootOrder":2,"size":1}]}`
                          type: string
                        diskSize:
                          description: |-
                            Disk size if using managed disk. For Rancher v2.3.x and above. Default 30 (string)
                            Disk size (in GiB)
                          type: string
                        imageName:
                          description: |-
                            (Deprecated) Use disk_info instead
                            Image name
                          type: string
                        memorySize:
                          description: |-
                            Memory size (in GiB), Default 4 (string)
                            Memory size (in GiB)
                          type: string
                        networkData:
                          description: |-
                            NetworkData content of cloud-init, base64 is supported (string)
                            NetworkData content of cloud-init, base64 is supported
                          type: string
                        networkInfo:
                          description: |-
                            A JSON string specifying info for the networks e.g. {\"interfaces\":[{\"networkName\":\"harvester-public/vlan1\"},{\"networkName\":\"harvester-public/vlan2\"}]} (string)
                            A JSON string specifying info for the networks e.g. `{"interfaces":[{"networkName":"harvester-public/vlan1"},{"networkName":"harvester-public/vlan2"}]}`
                          type: string
                        networkModel:
                          description: |-
                            (Deprecated) Use network_info instead
                            Network model
                          type: string
                        networkName:
                          description: |-
                            (Deprecated) Use network_info instead
                            Network name
                          type: string
                        sshUser:
                          description: |-
                            Set the name of the ssh user (string)
                            SSH username
                          type: string
                        userData:
                          description: |-
                            UserData content of cloud-init, base64 is supported. If the image does not contain the qemu-guest-agent package, you must install and start qemu-guest-agent using userdata (string)
                            UserData content of cloud-init, base64 is supported. If the image does not contain the qemu-guest-agent package, you must install and start qemu-guest-agent using userdata
                          type: string
                        vmAffinity:
                          description: |-
                            Virtual machine affinity, only base64 format is supported. For Rancher v2.6.7 and above (string)
                            VM affinity, base64 is supported
                          type: string
                        vmNamespace:
                          description: |-
                            Virtual machine namespace e.g. default (string)
                            Virtual machine namespace
                          type: string
                      type: object
                    type: array
                  hetznerConfig:
                    description: Hetzner config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        image:
                          description: |-
                            Azure virtual machine OS image. Default canonical:ubuntu-24_04-lts:server-gen1:latest (string)
                            Hetzner Cloud server image
                          type: string
                        networks:
                          description: |-
                            Comma-separated list of network IDs or names which should be attached to the server private network interface (string)
                            Comma-separated list of network IDs or names which should be attached to the server private network interface
                          type: string
                        serverLabels:
                          additionalProperties:
                            type: string
                          description: |-
                            Map of the labels which will be assigned to the server. This argument is only available on Hetzner Docker Node Driver:v3.6.0 and above (map)
                            Map of the labels which will be assigned to the server
                          type: object
                          x-kubernetes-map-type: granular
                        serverLocation:
                          description: |-
                            Hetzner Cloud datacenter. Default nbg1 (string)
                            Hetzner Cloud datacenter
                          type: string
                        serverType:
                          description: |-
                            Hetzner Cloud server type. Default cx11 (string)
                            Hetzner Cloud server type
                          type: string
                        usePrivateNetwork:
                          description: |-
                            Use private network. Default false (bool)
                            Use private network
                          type: boolean
                        userdata:
                          description: |-
                            Path to file with cloud-init user data (string)
                            Path to file with cloud-init user-data
                          type: string
                        volumes:
                          description: |-
                            Comma-separated list of volume IDs or names which should be attached to the server (string)
                            Comma-separated list of volume IDs or names which should be attached to the server
                          type: string
                      type: object
                    type: array
                  id:
                    description: (Computed) The ID of the resource (string)
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      Labels for Node Template object (map)
                      Labels of the resource
                    type: object
                    x-kubernetes-map-type: granular
                  linodeConfig:
                    description: Linode config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        authorizedUsers:
                          description: |-
                            Linode user accounts (seperated by commas) whose Linode SSH keys will be permitted root access to the created node. (string)
                            Linode user accounts (seperated by commas) whose Linode SSH keys will be permitted root access to the created node
                          type: string
                        createPrivateIp:
                          description: |-
                            Create private IP for the instance. Default false (bool)
                            Create private IP for the instance
                          type: boolean
                        dockerPort:
                          description: |-
                            Port number for Docker engine. Default 2376 (string)
                            Docker Port
                          type: string
                        image:
                          description: |-
                            Azure virtual machine OS image. Default canonical:ubuntu-24_04-lts:server-gen1:latest (string)
                            Specifies the Linode Instance image which determines the OS distribution and base files
                          type: string
                        instanceType:
                          description: |-
                            AWS instance type. Default t2.micro (string)
                            Specifies the Linode Instance type which determines CPU, memory, disk size, etc.
                          type: string
                        label:
                          description: |-
                            Linode Instance Label. (string)
                            Linode Instance Label
                          type: string
                        region:
                          description: |-
                            AWS region. (string)
                            Specifies the region (location) of the Linode instance
                          type: string
                        sshPort:
                          description: |-
                            SSH port. Default 22 (string)
                            Linode Instance SSH Port
                          type: string
                        sshUser:
                          description: |-
                            Set the name of the ssh user (string)
                            Specifies the user as which docker-machine should log in to the Linode instance to install Docker.
                          type: string
                        stackscript:
                          description: |-
                            Specifies the Linode StackScript to use to create the instance. (string)
                            Specifies the Linode StackScript to use to create the instance
                          type: string
                        stackscriptData:
                          description: |-
                            A JSON string specifying data for the selected StackScript. (string)
                            A JSON string specifying data for the selected StackScript
                          type: string
                        swapSize:
                          description: |-
                            Linode Instance Swap Size (MB). Default 512 (string)
                            Linode Instance Swap Size (MB)
                          type: string
                        tags:
                          description: |-
                            AWS Tags (e.g. key1,value1,key2,value2) (string)
                            A comma separated list of tags to apply to the the Linode resource
                          type: string
                        uaPrefix:
                          description: |-
                            Prefix the User-Agent in Linode API calls with some 'product/version' (string)
                            Prefix the User-Agent in Linode API calls with some 'product/version'
                          type: string
                      type: object
                    type: array
                  name:
                    description: The name of the Node Template (string)
                    type: string
                  nodeTaints:
                    description: Node taints. For Rancher v2.3.3 and above (List)
                    items:
                      properties:
                        effect:
                          description: 'Taint effect. Supported values : "NoExecute"
                            | "NoSchedule" | "PreferNoSchedule" (string)'
                          type: string
                        key:
                          description: Taint key (string)
                          type: string
                        timeAdded:
                          description: Taint time added (string)
                          type: string
                        value:
                          description: Taint value (string)
                          type: string
                      type: object
                    type: array
                  opennebulaConfig:
                    description: Opennebula config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        b2dSize:
                          description: Size of the Volatile disk in MB - only for
                            b2d (string)
                          type: string
                        cpu:
                          description: CPU value for the VM (string)
                          type: string
                        devPrefix:
                          description: 'Dev prefix to use for the images. E.g.: ''vd'',
                            ''sd'', ''hd'' (string)'
                          type: string
                        disableVnc:
                          description: VNC is enabled by default. Disable it with
                            this flag (bool)
                          type: boolean
                        diskResize:
                          description: Size of the disk for the VM in MB (string)
                          type: string
                        imageId:
                          description: Image ID to use as the VM OS. Conflicts with
                            image_name (string)
                          type: string
                        imageName:
                          description: (Deprecated) Use disk_info instead
                          type: string
                        imageOwner:
                          description: Owner of the image to use as the VM OS (string)
                          type: string
                        memory:
                          description: Size of the memory for the VM in MB (string)
                          type: string
                        networkId:
                          description: Opennebula network ID to connect the machine
                            to. Conflicts with network_name (string)
                          type: string
                        networkName:
                          description: (Deprecated) Use network_info instead
                          type: string
                        networkOwner:
                          description: Opennebula user ID of the Network to connect
                            the machine to (string)
                          type: string
                        sshUser:
                          description: Set the name of the ssh user (string)
                          type: string
                        templateId:
                          description: Opennebula template ID to use. Conflicts with
                            template_name (string)
                          type: string
                        templateName:
                          description: Name of the Opennbula template to use. Conflicts
                            with template_id (string)
                          type: string
                        user:
                          description: Set the user for the XML-RPC API authentication
                            (string)
                          type: string
                        vcpu:
                          description: VCPUs for the VM (string)
                          type: string
                        xmlRpcUrl:
                          description: Set the url for the Opennebula XML-RPC API
                            (string)
                          type: string
                      type: object
                    type: array
                  openstackConfig:
                    description: Openstack config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        activeTimeout:
                          description: OpenStack active timeout Default 200 (string)
                          type: string
                        applicationCredentialId:
                          description: OpenStack application credential id. Conflicts
                            with application_credential_name (string)
                          type: string
                        applicationCredentialName:
                          description: OpenStack application credential name. Conflicts
                            with application_credential_id (string)
                          type: string
                        authUrl:
                          description: OpenStack authentication URL (string)
                          type: string
                        availabilityZone:
                          description: OpenStack availability zone (string)
                          type: string
                        bootFromVolume:
                          description: Enable booting from volume. Default is false
                            (bool)
                          type: boolean
                        cacert:
                          description: CA certificate bundle to verify against (string)
                          type: string
                        configDrive:
                          description: Enables the OpenStack config drive for the
                            instance. Default false (bool)
                          type: boolean
                        domainId:
                          description: OpenStack domain ID. Identity v3 only. Conflicts
                            with domain_name (string)
                          type: string
                        domainName:
                          description: OpenStack domain name. Identity v3 only. Conflicts
                            with domain_id (string)
                          type: string
                        endpointType:
                          description: OpenStack endpoint type. adminURL, internalURL
                            or publicURL (string)
                          type: string
                        flavorId:
                          description: OpenStack flavor id to use for the instance.
                            Conflicts with flavor_name (string)
                          type: string
                        flavorName:
                          description: OpenStack flavor name to use for the instance.
                            Conflicts with flavor_id (string)
                          type: string
                        floatingIpPool:
                          description: OpenStack floating IP pool to get an IP from
                            to assign to the instance (string)
                          type: string
                        imageId:
                          description: Image ID to use as the VM OS. Conflicts with
                            image_name (string)
                          type: string
                        imageName:
                          description: (Deprecated) Use disk_info instead
                          type: string
                        insecure:
                          description: Disable TLS credential checking. Default false
                            (bool)
                          type: boolean
                        ipVersion:
                          description: OpenStack version of IP address assigned for
                            the machine Default 4 (string)
                          type: string
                        keypairName:
                          description: OpenStack keypair to use to SSH to the instance
                            (string)
                          type: string
                        netId:
                          description: OpenStack network id the machine will be connected
                            on. Conflicts with net_name (string)
                          type: string
                        netName:
                          description: OpenStack network name the machine will be
                            connected on. Conflicts with net_id (string)
                          type: string
                        novaNetwork:
                          description: Use the nova networking services instead of
                            neutron (string)
                          type: boolean
                        region:
                          description: AWS region. (string)
                          type: string
                        secGroups:
                          description: OpenStack comma separated security groups for
                            the machine (string)
                          type: string
                        sshPort:
                          description: SSH port. Default 22 (string)
                          type: string
                        sshUser:
                          description: Set the name of the ssh user (string)
                          type: string
                        tenantId:
                          description: OpenStack tenant id. Conflicts with tenant_name
                            (string)
                          type: string
                        tenantName:
                          description: OpenStack tenant name. Conflicts with tenant_id
                            (string)
                          type: string
                        userDataFile:
                          description: File containing an openstack userdata script
                            (string)
                          type: string
                        username:
                          description: OpenStack username (string)
                          type: string
                        volumeDevicePath:
                          description: OpenStack volume device path (attaching). Applicable
                            only when boot_from_volume is true. Omit for auto /dev/vdb.
                            (string)
                          type: string
                        volumeId:
                          description: OpenStack volume id of existing volume. Applicable
                            only when boot_from_volume is true (string)
                          type: string
                        volumeName:
                          description: OpenStack volume name of existing volume. Applicable
                            only when boot_from_volume is true (string)
                          type: string
                        volumeSize:
                          description: OpenStack volume size (GiB). Required when
                            boot_from_volume is true (string)
                          type: string
                        volumeType:
                          description: Amazon EBS volume type. Default gp2 (string)
                          type: string
                      type: object
                    type: array
                  outscaleConfig:
                    description: Outscale config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        extraTagsAll:
                          description: |-
                            Extra tags for all created resources (e.g. key1=value1,key2=value2) (list)
                            Extra tags for all created resources (e.g. key1=value1,key2=value2)
                          items:
                            type: string
                          type: array
                        extraTagsInstances:
                          description: |-
                            Extra tags only for instances (e.g. key1=value1,key2=value2) (list)
                            Extra tags only for instances (e.g. key1=value1,key2=value2)
                          items:
                            type: string
                          type: array
                        instanceType:
                          description: |-
                            AWS instance type. Default t2.micro (string)
                            Outscale VM type
                          type: string
                        region:
                          description: |-
                            AWS region. (string)
                            Outscale Region
                          type: string
                        rootDiskIops:
                          description: |-
                            Iops for io1 Root Disk. From 1 to 13000.
                            Iops for io1 Root Disk. From 1 to 13000.
                          type: number
                        rootDiskSize:
                          description: |-
                            Size of the Root Disk (in GB). From 1 to 14901.
                            Size of the Root Disk (in GB). From 1 to 14901.
                          type: number
                        rootDiskType:
                          description: |-
                            Type of the Root Disk. Possible values are :'standard', 'gp2' or 'io1'.
                            Type of the Root Disk. Possible values are :'standard', 'gp2' or 'io1'.
                          type: string
                        securityGroupIds:
                          description: |-
                            Ids of user defined Security Groups to add to the machine. (list)
                            Ids of user defined Security Groups to add to the machine
                          items:
                            type: string
                          type: array
                        sourceOmi:
                          description: |-
                            Outscale Machine Image to use as bootstrap for the VM. Default ami-2cf1fa3e (string)
                            Outscale Machine Image to use as bootstrap for the VM
                          type: string
                      type: object
                    type: array
                  useInternalIpAddress:
                    description: Engine storage driver for the node template (bool)
                    type: boolean
                  vsphereConfig:
                    description: vSphere config for the Node Template (list maxitems:1)
                    items:
                      properties:
                        boot2dockerUrl:
                          description: |-
                            vSphere URL for boot2docker iso image. Default https://releases.rancher.com/os/latest/rancheros-vmware.iso (string)
                            vSphere URL for boot2docker image
                          type: string
                        cfgparam:
                          description: |-
                            vSphere vm configuration parameters (used for guestinfo) (list)
                            vSphere vm configuration parameters (used for guestinfo)
                          items:
                            type: string
                          type: array
                        cloneFrom:
                          description: |-
                            If you choose creation type vm (clone vm) a name of what vm you want to clone is required. From Rancher v2.3.3 (string)
                            If you choose creation type clone a name of what you want to clone is required
                          type: string
                        cloudConfig:
                          description: |-
                            Cloud Config YAML content to inject as user-data. From Rancher v2.3.3 (string)
                            Filepath to a cloud-config yaml file to put into the ISO user-data
                          type: string
                        cloudinit:
                          description: |-
                            vSphere cloud-init file or url to set in the guestinfo (string)
                            vSphere cloud-init filepath or url to add to guestinfo
                          type: string
                        contentLibrary:
                          description: |-
                            If you choose to clone from a content library template specify the name of the library. From Rancher v2.3.3 (string)
                            If you choose to clone from a content library template specify the name of the library
                          type: string
                        cpuCount:
                          description: |-
                            CPU count, Default 2 (string)
                            vSphere CPU number for docker VM
                          type: string
                        creationType:
                          description: |-
                            Creation type when creating a new virtual machine. Supported values: vm, template, library, legacy. Default legacy. From Rancher v2.3.3 (string)
                            Creation type when creating a new virtual machine. Supported values: vm, template, library, legacy
                          type: string
                        customAttributes:
                          description: |-
                            vSphere custom attributes, format key/value e.g. 200=my custom value. From Rancher v2.3.3 (List)
                            vSphere custom attributes, format key/value e.g. '200=my custom value'
                          items:
                            type: string
                          type: array
                        datacenter:
                          description: |-
                            vSphere datacenter for docker VM (string)
                            vSphere datacenter for virtual machine
                          type: string
                        datastore:
                          description: |-
                            vSphere datastore for docker VM (string)
                            vSphere datastore for virtual machine
                          type: string
                        datastoreCluster:
                          description: |-
                            vSphere datastore cluster for virtual machine. From Rancher v2.3.3 (string)
                            vSphere datastore cluster for virtual machine
                          type: string
                        diskSize:
                          description: |-
                            Disk size if using managed disk. For Rancher v2.3.x and above. Default 30 (string)
                            vSphere size of disk for docker VM (in MB)
                          type: string
                        folder:
                          description: |-
                            vSphere folder for the docker VM. This folder must already exist in the datacenter (string)
                            vSphere folder for the docker VM. This folder must already exist in the datacenter
                          type: string
                        gracefulShutdownTimeout:
                          description: Duration in seconds before the graceful shutdown
                            of the VM times out and the VM is destroyed. A force destroy
                            will be performed when the value is zero
                          type: string
                        hostsystem:
                          description: |-
                            vSphere compute resource where the docker VM will be instantiated. This can be omitted if using a cluster with DRS (string)
                            vSphere compute resource where the docker VM will be instantiated. This can be omitted if using a cluster with DRS
                          type: string
                        memorySize:
                          description: |-
                            Memory size (in GiB), Default 4 (string)
                            vSphere size of memory for docker VM (in MB)
                          type: string
                        network:
                          description: |-
                            vSphere network where the docker VM will be attached (list)
                            vSphere network where the virtual machine will be attached
                          items:
                            type: string
                          type: array
                        pool:
                          description: |-
                            vSphere resource pool for docker VM (string)
                            vSphere resource pool for docker VM
                          type: string
                        sshPort:
                          description: |-
                            SSH port. Default 22 (string)
                            If using a non-B2D image you can specify the ssh port
                          type: string
                        sshUser:
                          description: |-
                            Set the name of the ssh user (string)
                            If using a non-B2D image you can specify the ssh user
                          type: string
                        sshUserGroup:
                          description: |-
                            If using a non-B2D image the uploaded keys will need chown'ed. Default staff. From Rancher v2.3.3 (string)
                            If using a non-B2D image the uploaded keys will need chown'ed, defaults to staff e.g. docker:staff
                          type: string
                        tags:
                          description: |-
                            AWS Tags (e.g. key1,value1,key2,value2) (string)
                            vSphere tags id e.g. urn:xxx
                          items:
                            type: string
                          type: array
                        username:
                          description: |-
                            OpenStack username (string)
                            vSphere username
                          type: string
                        vappIpAllocationPolicy:
                          description: |-
                            vSphere vApp IP allocation policy. Supported values are: dhcp, fixed, transient and fixedAllocated (string)
                            vSphere vApp IP allocation policy. Supported values are: dhcp, fixed, transient and fixedAllocated
                          type: string
                        vappIpProtocol:
                          description: |-
                            vSphere vApp IP protocol for this deployment. Supported values are: IPv4 and IPv6 (string)
                            vSphere vApp IP protocol for this deployment. Supported values are: IPv4 and IPv6
                          type: string
                        vappProperty:
                          description: |-
                            vSphere vApp properties (list)
                            vSphere vApp properties
                          items:
                            type: string
                          type: array
                        vappTransport:
                          description: |-
                            vSphere OVF environment transports to use for properties. Supported values are: iso and com.vmware.guestInfo (string)
                            vSphere OVF environment transports to use for properties. Supported values are: iso and com.vmware.guestInfo
                          type: string
                        vcenter:
                          description: |-
                            vSphere IP/hostname for vCenter. Mandatory on Rancher v2.0.x and v2.1.x. Use rancher2_cloud_credential from Rancher v2.2.x (string)
                            vSphere IP/hostname for vCenter
                          type: string
                        vcenterPort:
                          description: |-
                            vSphere Port for vCenter. Mandatory on Rancher v2.0.x and v2.1.x. Use rancher2_cloud_credential from Rancher v2.2.x. Default 443 (string)
                            vSphere Port for vCenter
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
